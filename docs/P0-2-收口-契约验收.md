# P0-2 收口：数据契约治理完成

> **状态：P0-2 CLOSED（2026-08-23）**
>
> A = State（Canonical State）· B = Evidence（Evidence Resolver）· C = Judgment（AI Judgment Persistence）
>
> 三块基础设施已封板。下一阶段不再修改底层真源，进入 **Current Judgment Read Model / Consumer Layer（P0-3）**。

---

## ① 静态契约验收（2026-08-23，代码级核查）

### A. State —— stage 无第二真源 ✅

| 核查点 | 结果 |
|---|---|
| stage 写者收口 | 合法写者 4 个：classifier / intent / manual / deal rule（`legalStageWriters.ts` 收口元数据）。insightService 已禁写（`salesInsightWrite` 只写 signal），generic upsert 已剥离（`customerUpsertPolicy` 运行时 + 类型双保险） |
| import 路径 | `importCustomerFromProfile` 只写 `account.sales_stage`（盘点 L74 已登记投影列，单向、不碰 `customer_profile.stage`）——不构成绕道 |
| stage 消费者 | funnel 读 `customer_profile.stage` 经 `stageToFunnel` 归一；`getCanonicalState`（完整读模型）暂无消费者——消费层未建，属预期（P0-3 消费） |
| 禁 stage 硬门禁 | `customer_judgment` 表 CHECK 拦截 + TS 层 `isCustomerJudgmentType` 双拦截 |

### B. Evidence —— 无裸 key 新写点 ✅

| 核查点 | 结果 |
|---|---|
| messageKey 构造集中 | 唯一构造点 `shared/messageKey.ts`（`buildMessageKey`），`salesFollowUpService` 等统一调用；全仓无散落 `local:msg` 拼串 |
| 写层不构造 | `salesDbService.todoCreate`（写层）只接受参数，不生成 key |
| 证据诚实 | 无可靠 key → `message_key=null`，状态派生 `unavailable`，无伪造路径（C.2/C.3 双测试覆盖） |

### C. Judgment —— 四字段无现场生成不落库路径 ✅

| 核查点 | 结果 |
|---|---|
| summary | 唯一生产者 `generateInsightForSession`（全部触发路径：定时/事件/批量画像均从函数内部落库，只接一个入口） |
| opportunity/risk/next_action | 唯一生产者 `generateActionAnalysis`，三个调用点（预热 / suggest / 客户 360）全部统一落库 |
| UI 直读 | `src/` 无 `customer_judgment` 引用（消费层未建，属预期） |
| UI 自解释 | UI 不现场调 LLM 当真源；stage 一律走 IPC 归一响应（suggest 为用户主动触发，主进程生成） |

**结论：A/B/C 三块之间无重新出现的第二套真源。**

---

## ② 真实 DB 只读盘点（2026-08-23 20:19 后快照，`~/Library/Application Support/weflow/weflow-sales.db`）

> 脚本：`scripts/p0-2-real-db-audit.ts`（sql.js 内存加载，纯只读，可重复运行）

| 维度 | 数值 |
|---|---|
| 客户总数（customer_profile） | **200** |
| canonical stage 分布 | contacted=76 · quoted=67 · won=26 · lost=21 · negotiating=9 · **unknown=1**（六档齐全，几乎零脏值） |
| intent_tag_log | 1131 条（今日写入 1 条，**带 message_key=1/1 ✓**，evidence_text=`价格多少` 客户原话 ✓，P0-1 透传真实生效） |
| follow_up_task | 2524 条；今日 15 条全部无 source_message_id（观察，见下） |
| customer_judgment | **表不存在** → 判断链尚未在真实运行中激活（应用最后一次启动于 P0-2C.1 提交之前；建表随启动 `CREATE TABLE IF NOT EXISTS` 自动完成） |

### 关键解读

1. **200 客户六档 stage 分布健康**：P0-2A 口径归一已收敛存量脏值（仅 1 个 unknown）。
2. **P0-1 透传样本验证**：intent_tag_log 今日写入完整携带 messageKey（WCDB 路径格式，P0-2B 可解析）+ 客户原话 evidence，证明证据链在真实写入路径有效。
3. **观察（非 P0-2 范围，记录不修复）**：
   - `follow_up_task.source_message_id` 写点在任务触发路径（R0 规则等）尚未实际生效（今日 0/15，重启后 147 条新任务仍 0 锚点）；P0-2C.3 证据解析器已有兜底（任务无 key → chatService 最近消息 key），判断链不受影响。建议归入 P0-4 Action 埋点/行动结果回流时一并治理。
   - evidence 质量：存在 evidence_text=`[视频]` 的样本（extractEvidence 取最近一条实质消息，视频消息非文本依据）——P0-3 消费层设计时可将图片/视频消息视为非文本证据（提示性，不阻断）。

### 运行时验证（2026-08-23 21:0x，应用以 P0-2C+ 代码重启后）—— **P0-2 runtime CLOSED** ✅

| 验收项 | 结果 |
|---|---|
| `customer_judgment` 表自动建表 | ✅ 随启动 `CREATE TABLE IF NOT EXISTS` 生成 |
| DB CHECK 禁 stage 硬门禁 | ✅ 生效 |
| 判断真实产出 | ✅ **18 条**（6 客户 × opportunity/risk/next_action），source=ai，model=deepseek-v4-flash（PRD§23 溯源） |
| evidence 锚点 | ✅ 100% 带 message_key，**6/6 P0-2B 可解析**（WCDB 3 段格式→localId）；evidence_text=客户原话（如「不管多少钱，没问题就行」） |
| basis 输入快照 | ✅ 含 taskId/triggerType/channel |
| 去重健康 | ✅ 0 重复组合、0 24h 窗口重复（append-only + 按类型去重真实生效） |
| 冲突观察 | ✅ 0 冲突（risk×won=0 / opportunity×lost=0 / orphan=0） |
| summary 判断 | ⏳ 0 条（见解链依赖 urge/画像触发时机，尚未触发；非缺陷，随运行继续产出） |
| 判断客户覆盖 | 6/200（3%）= 预热高/紧急任务池，与设计一致（preheat 前 8 条） |

**结论：P0-2 完成真实运行态验收——判断链在真实数据上产出正确、证据诚实、去重与护栏全部生效。剩余 summary 覆盖随运行自然累积，由同一盘点脚本持续可查。**

---

## ③ 架构护栏（P0-2 封死条款）

以下条款永久生效，新代码不得违反：

```
❌ 通用 upsert 不写 stage（customerUpsertPolicy 已剥离，禁止恢复）
❌ insightService 不写 stage（只写 signal；禁止抢 stage）
❌ 不新增 customer_judgment.stage（TS 守卫 + DB CHECK 双拦截，禁止以任何形式绕过）
❌ 判断落库必须能回答"为什么"（type/value/generatedAt/model/source/evidenceText/messageKey；
   无可靠 messageKey 宁可标 unavailable，绝不伪造）
❌ judgment 不 UPDATE 历史（append-only；新判断=新记录，去重走 hasRecentJudgment 窗口）
❌ 新业务代码不得重新造 messageKey（唯一构造点 shared/messageKey.ts buildMessageKey）
❌ UI 不直接解释 profile.stage / 不现场调 LLM 作为唯一真源（消费走 canonical 读模型 + IPC 归一响应）
❌ AI 判断不冒充客户事实信号（正则机会/风险=事实；AI judgment=解释；follow_up_task=行动对象，三层互不冒充）
```

---

## ④ 下一步：P0-3 Current Judgment Consumer Layer

`getCustomerCurrentView()` 组装三块真源为单一客户当前视图：

```
Canonical State（现在是什么状态）
   +
Evidence（为什么：judgmentEvidenceStatus + P0-2B 证据回查）
   +
Judgment（AI 当前怎么看：judgmentCurrentAll）
   ↓
getCustomerCurrentView()  ——  唯一消费入口
   ↓
UI（AI 当前判断卡 + 证据链）
```

**铁律（延续收口）**：UI 只消费这一个视图，不再从七张表自己拼；底层真源在 P0-2 之后不再修改（除非 P0-4 之后的治理需求）。
