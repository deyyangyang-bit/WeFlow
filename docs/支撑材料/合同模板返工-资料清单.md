# 合同/报价单/发票模板返工 · 状态

> 2026-09-02 更新:根因已定位并修复,模板本身无需返工。

## 当前状态

- ✅ 正确格式样例已收到(2026-09-02):真实报价单/销售合同×2/开票申请单
- ✅ 现有模板 `resources/crm-templates/{quotation,contract}.docx` 逐项比对为忠实复刻(字体/页边距/Logo/文案/售后附件),无需返工
- ✅ 根因确认:「文档格式不对」不是模板错 — 是打包态模板路径 bug
  - `crmDocGenService.ts` 打包态候选路径缺 `resources` 段 → 三个候选全 miss
  - 静默 fallback 到 userData 占位模板 → 用户看到的是简陋占位文档,不是真模板
- ✅ 修复:打包路径加 `resources` 段;删除占位模板机制(ensureTemplates 预埋 + fallback),模板缺失时响亮失败(返回 {ok:false, reason})
- ✅ 验证:tsc 零错误;docgen 测试 68/68;真实模板 + 生产渲染函数生成 3 份测试文档通过
- ✅ 开票申请单货款栏对齐真实用法:收款日期默认「未收款」、勾选框留空(先开票后付款);有收款日期才打 R 并填日期(数据形状预留 paid_date)
- ⬜ 剩余:重新打包安装后,用真实记录走一遍生成 → 交付,确认打包态模板加载成功