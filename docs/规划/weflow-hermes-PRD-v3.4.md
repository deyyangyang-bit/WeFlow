# WeFlow × Hermes 需求文档（PRD v3.4）

| | |
|---|---|
| 版本 | v3.4 |
| 日期 | 2026-09-14（实施状态校准；原始评审版 2026-09-02） |
| 状态 | 现行执行版（P0/P1/P2 已进入实现与验收；**Phase 3a 代码侧已实现，部署与真机验收未做——代码侧仍未收口**；Phase 3b / Phase 4 未启动；管理层最终签字待补） |
| 前置文档 | 总体规划 v3 冻结版（XMind） |

> 本文档是开发执行依据。架构概念见规划导图，本文只写：做什么、谁用、怎么算做完、有什么风险。

**v3.4 修订**：以 v3.3 为基准，合并回 v3.1 已有但被覆盖的内容——身份认证分层（§3.1）、本地身份档案（1.2a）、设备级周期提醒（1.7a）、R12 经销商拿货预警（1.7b）、交付与设备字段（§5.1）、间接信号提取与信息缺口反问卡（2.4）、Phase 4 经销商维护深化与产品口径备忘（§8）；修复两处内部矛盾——封面版本号统一、§9 迁移表知识条目 authority 与 2.7 对齐（community + staging 待审）。bot 策略维持 v3.3 不变：官方单向推送，不做双向。

**v3.3 修订**：按 2026-09-02 六条方向性决策校准：分配制 / 知识库上 NAS / AI 画像双存档 / 文档生成修复 / 商机评测前置 / 首触 SLA 改 LLM 对话扫描；新增 Hermes 微信单向推送（2.11）；补实测基线（§1、§12）

**2026-09-12 修订（中央节点部署形态）**：中央节点由「NAS」改为**阶段一独立主机 / 阶段二按需升 NAS** 两段式——本文档后续所有「中央主机」均指该节点的当前形态（阶段一 = 独立主机）。同步修订 §2 拓扑、§3.1 认证分层、§5 需求项 1.1/1.2a、§6 需求项 2.7、§7 Phase 3a、§10 风险 R3/R5、§11 ADR-001、§13 里程碑。采购规格与实施细节见 [中央节点采购方案](中央节点采购方案.md)。本次复核将 R3 统一为四项上线保护，补齐一致性备份与停写迁移，并明确 NAS 不豁免运维责任。

**2026-09-14 实施状态校准**：用户确认中央机已由部署负责人完成基础安装部署；本结论只代表硬件、系统与基础容器环境已经就位，不等于 Phase 3a 中央业务服务、HTTP 同步、企业认证、冲突裁决或 Phase 3b WeKnora 业务接入已经完成。P0/P1/P2 已在 2026-09-12～14 持续更新，后续 Phase 3/4 必须继承 §7.1 的现有真源和写入纪律，不得重新建立平行语义。实际机型、RAID/UPS、备份、告警及恢复演练证据由部署负责人回填《中央节点采购方案》§9。

**2026-09-14 Phase 3a 首轮实现**：中央服务与 Electron 双向同步适配完成实现与自动化验证（见 §7 Phase 3a「3a 实施状态」及 `docs/实施记录/中央节点同步通道-实施记录-claude-20260914.md`）。此结论**仅指代码**：真实 PostgreSQL 端到端、反向代理与证书、双机同步演练、离职移交演练、上行延迟实测与 Windows 打包均**未执行**，不得描述为通过。

**2026-09-15 Phase 3a 阻断项修复**：首轮实现暴露的八类 P0/P1 阻断项（敏感字段出机、同工作区跨设备越权、既有 outbox 未真正接通、版本化增量漏更新、过滤行卡游标、中央引用不统一、下行命令校验过弱、缺真实端到端契约测试）已完成修复并通过自动化验证。**此结论仍仅指代码**：部署与真机验收一条未做（原始清单同上，另含 `docker build`），且**下行指令的线索档案面**、**冲突裁决未细化**两项残留已如实披露。审计报告见 `docs/audit/中央同步-阻断项修复-审计报告-claude-20260915.md`。**不得据此写成「Phase 3a 已收口」或「全项目代码已完成」。**

**2026-09-15 Phase 3a 复核收口（5 个缺口）**：同日复核又发现 5 个代码/测试缺口并已收口——①`transfer` 中央指令链此前实际断裂（`commandPayloadOf()` 无 `transfer` 分支，outbox 直接落 `failed`），且 e2e 头部「覆盖 transfer」的声明不实；现补齐分支并实现**双目标投递**（新归属 `apply` / 原归属 `remove`，**两个目标都被中央受理才结算 `sent`**，部分成功靠顺序重试 + 中央幂等收敛），e2e 补真实 `transferAssignment()` 全链与真实 `runSla1Recycle()` 回收链；②中央 HTTP 下行 lead 白名单由 8 字段收紧为**按传输上下文分档**（中央 6 字段、SMB 保留 Phase 1 的 8 字段历史口径）；③`/sync/push` 补服务端**实体引用闸门**（实体类别 + 载荷 `*Ref` 形态/类别/命名空间归属）；④补齐**中央 append-only 审计**（邀请码签发、下行指令首次受理，与业务同事务，**幂等重放不追加**）；⑤管理员吊销的畸形 UUID 由 500 改为 **400 E101 且不碰数据库**。**此结论仍仅指代码**：中央侧全部跑在 `MemoryCentralStore` 上，**真实 PostgreSQL 未验证**（PG 侧只有源码级契约断言），`docker build`、双机、Windows、SSE、真实消息推送、反向代理与证书**全部未执行**。**Phase 3a 代码侧仍未收口。**

**2026-09-15 Phase 3a 契约修复（移交 SLA / SMB 校验 / 建档字段）**：同日第三轮——①修复 **transfer 丢失 SLA1** 的真实缺陷：outbox 与中央指令此前不携带 `sla1Deadline`/`mode`，接收端 `sla1_deadline` 落 NULL；现发起端同事务写入、指令必填（`sla1Deadline` 必须有限正整数）、接收端**精确按指令值落地不重算**、`remove` 不动 SLA、重放零漂移；②SMB `validateDownEventFile()` **真正接入** `validateDownCommand(subject, 'smb')`（此前合法信封配空 payload 也能进状态机；SMB 保留 8 字段 lead、目标存在性用已验证的本机投递键证明，不伪造 UUID）；③中央 assign/transfer **建档字段强制**：lead 必须含最小身份（`leadId` 正整数 / `contactType` 枚举 / `contactNormalized` 非空），中央 HTTP 固定 6 字段全部存在，顶层与子对象 `leadId` 不一致即拒收，被拒指令不消耗幂等键；④双目标 4xx 部分成功：整行 `failed` + 审计补 `failedTarget`，网络/5xx 仍 `pending` 靠幂等重试收敛。**边界同上**：仍全部跑在 `MemoryCentralStore` 与隔离临时目录上，真实 PostgreSQL / Docker / 双机 / Windows / SSE / 真实消息与 AI 调用**均未验证**；下行仍携带 `contactNormalized`，**不声称「下行零身份值」**。**Phase 3a 代码侧仍未收口。**

---

## 1. 背景与目标

WeFlow 现状：单机 AI 销售助手（Electron），已具备微信聊天解密存档、客户 360、阶段分类、行动引擎 R0-R8、线索池、跟单中心、关键词知识库等 28 项能力。

**实测基线（2026-09-02 数据库审计）**：被动接收链健康——解析微信消息 5.16 万条、银行到款识别与手动认领运转中、物流签收认领 420 条。主动经营链几乎未运转——4,680 条线索首触仅 1 条、行动卡执行率 0.6%（12,952 张任务卡执行 80 张）、报价 0 / 合同 1 / 商机 5、知识库 219 条零增长。结论：**先把「分配 + 跟进」主动链跑起来，再谈 AI 深化。**

本项目的业务目标（非技术目标）：

1. **客户资源不流失**——资源集中分配、超时回收、离职可交接
2. **跟进不漏**——加好友、首触、回访、拿货、停滞全链路有提醒兜底
3. **知识不依赖老员工**——聊天中的经验经审核沉淀为团队知识
4. **管理看得见**——归属、阶段、成交、报价可追溯

非目标（明确不做）：不替代销售聊天、AI 不自动对外发消息、聊天记录原文不上中央主机、不做通用办公平台。

---

## 2. 部署拓扑（本项目的地基事实）

**现实拓扑：分散单机 + 多微信号。**

- 每个销售一台电脑，登自己的工作微信；每个销售可能有多个工作微信号
- WeFlow 按微信号分库（现有机制），一台机器可挂多个微信号分库
- 加好友、聊天等检测**只能在销售本机**完成（中央无法感知销售手机上的微信）

因此架构分工为。下图是 Phase 3 目标拓扑；截至 2026-09-14，中央机基础安装已完成，但 WeFlow 仍运行 Phase 1 SMB 同步，图中的中央业务服务和 HTTP API 尚待开发：

```
┌─ 中央主机（独立部署）───────────────────────┐
│  轻量分配服务：资源池表 + 分配/回收/SLA 计时     │
│  （一张表 + 两个接口，非同步引擎）              │
│  后期叠加：PostgreSQL 档案库 / WeKnora 知识库    │
└──────────────────┬──────────────────────────┘
                   │ HTTP API
        ┌──────────┼──────────┐
        ▼          ▼          ▼
     销售甲机    销售乙机    销售丙机
     ├微信号1   ├微信号1
     ├微信号2   └微信号2
     └微信号3
     本机职责：拉取本人分配 / 扫全部微信号分库
     检测加好友 / 上报状态 / AI 建档 / 提醒执行
```

**铁律：计时与裁决在中央，检测与执行在本地。** 中央服务只有分配域，不构成对销售机的实时运行依赖（中央主机离线时销售端照常工作，状态恢复后补报）。

---

## 3. 角色与权限总表

| 角色 | 客户数据 | 资源分配 | 知识库 | 成本价 | 治理操作 |
|---|---|---|---|---|---|
| 销售 | 本人客户全部（多微信号归并） | ❌ 只读本人分配情况 | 读已发布 | ❌ | 提提案 |
| 分配员（权限组，默认主管兼任） | 同主管 | ✅ 录入/分配/调比例/回收 | 同主管 | 同主管 | — |
| 销售主管 | 团队只读（阶段与成交，**不看聊天原文**） | ✅ | 读 + 审核 | ✅ | 审核/移交审批/仲裁 |
| 管理员 | 归属管理 | ✅ + 规则配置 | 全部 | ✅ | 账号/角色/密钥 |
| 系统/AI 账号 | 仅经 Tool Gateway 按调用者身份 | ❌ 永不授予 | 禁发布 | 受 ACL | 无人工登录 |

### 3.1 身份认证分层（不做传统登录界面）

| 层 | 阶段 | 形态 | 状态 |
|---|---|---|---|
| 防窥探 | 已有 | 应用锁（密码锁屏，`LockScreen` + 设置页开关 + 侧边栏一键锁定） | ✅ 已实现，单机自管，离职交接改密码即可，无中央回收负担 |
| 员工身份 | Phase 1 | **本地身份档案**（需求项 1.2a），非登录界面 | ✅ 已实现身份 IPC、设置页、署名与审计；待各销售机建档验收 |
| 企业认证 | Phase 3a | **中央主机工作区绑定**（见 Phase 3a） | 🟡 代码已实现（邀请码一次性认领 + 设备凭证 + 轮换/吊销 + 五角色服务端权限 + 跨设备越权拒绝），**待部署验收——代码侧仍未收口** |

**为什么 wxid ≠ 员工身份**：一人多号（3 个 wxid 同一人）、换号（wxid 变了人没变）、离职交接（wxid 没变人变了）。wxid 只作设备/数据源标识，员工身份独立建档。

---

## 4. Phase 0 · 数据宪法（1~2 周）

### 交付物

`DATA-CONSTITUTION.md` + 建表 DDL + 迁移脚本骨架。**本阶段不含存量数据迁移**（迁移属 Phase 1 第一周）。

### 内容范围

- 11 个核心对象：`customer / customer_identity / lead / assignment / ownership / ownership_history / stage_history / evidence / outbox_event / audit_event / opportunity`
- `quote`（报价）直接入宪——Phase 1 报价版本管理要用
- `action / ticket` 列为"扩展对象"，入宪时机：Phase 2 / Phase 4
- 两项 Policy：Identity Resolution（手机号优先 / wxid 兜底 / 后补关联；AI 只可提合并提案）；Stage Transition（允许跳级、回退受限、成交禁直连、流失复活需确认）
- SSOT 规则 + Soft Delete 规则 + Feature Gate 七问，同步写入仓库 `AGENTS.md` 铁律区
- AI 字段三档：A 类 auto_committed（依据 = 证据质量 + 规则校验，LLM 置信度仅辅助）/ B 类 proposed→confirmed / C 类 request→审批

### 验收标准（DoD）

1. 11+1 对象 DDL 评审通过，字段含 source / updated_by / updated_at / version / 删除标记
2. 销售主管确认 Identity / Ownership 两章符合业务实际
3. 契约导入现有库成功（空表结构可建）
4. `AGENTS.md` 铁律区更新完成

---

## 5. Phase 1 · 单机可靠性（约 3 周，重排后）

> 原 2 周估算不现实，重排为 3 周并排序：备份 → 迁移 → 分配 → 售后规则。

### 需求项

| # | 需求 | 要点 |
|---|---|---|
| 1.1 | **自动备份**（第一优先级） | 本机双库加密备份 → 中央主机（未就位前：外接硬盘/另一台机，改路径切换）。每日增量 + 每周全量；恢复演练一次 |
| 1.2 | **存量数据迁移** | 现有客户/线索/历史成交映射进 11 对象；待导入的存量线索（含奋斗群批量线索约 4,281 条）走查重后进资源池；迁移报告（成功/失败/冲突数） |
| 1.2a | **本地身份档案**（分配前置） | 首次启动/设置页建立：姓名 + 角色（销售/主管/分配员），存本地配置，与现有应用锁完全独立。用途：`audit_event` actor 署名、分配服务认领身份、审批确认显示操作人。**⚠️ 角色字段仅作署名，不作访问控制依据**（Phase 1 自我声明即使填主管也无团队数据可看——数据按微信号分库，池子与分配台在分配端机器上；真正权限闸门 = Phase 3a 中央主机服务端绑定认证，主管/分配员/管理员多一步验证）。Phase 3a 绑定中央主机时平滑升级为企业账号，档案字段不变 |
| 1.3 | **客户资源分配** | 分配员录入（Excel/粘贴导入、自动查重）→ 三模式分配（比例权重默认/轮询/负载）→ 销售机拉取 → 两段接力 SLA → 回收改派。4,281 条群扫线索只有昵称/联系方式、无客户名称资料：**先小批量试分配（约 200 条）验证可加率与首触率，达标再放量；不达标先补数据质量，不整批下发** |
| 1.4 | 两段接力 SLA | 第一段"加了没有"：分配后 24h 未加好友 → 提醒，每 24h 复查，第 3 次抄送主管，3 次后回收改派他人。第二段"聊了没有"：**弃用首触 SLA 计时器，改为 LLM 扫描对话判断跟进状态**（是否已有效触达 / 是否需介入），低置信转人工；第一段"加了没有"无对话可扫，保留机械计时 + 检测 |
| 1.4a | 加好友判定（双路） | ① 自动：本机扫全部微信号分库匹配手机号/wxid；② 手动：销售在资源卡「绑定微信」——昵称关键词搜索 → 下拉选本机联系人（多微信号分库合集）→ 确认。内部绑 wxid（昵称仅显示用，改名不失效）。任一命中即停表。绑定写入 customer_identity（source=manual, confidence=1.0），全程审计留痕 |
| 1.5 | 客户类型字段 | dealer / end_user，R9/R10 的前置 |
| 1.6 | 售后生命周期 | 每笔成交独立走：成交 → 已交付 → 待回访 → 复购/老客（规则驱动）；复购客户叠加客户级关系维护（升 A 级、回访频率提高） |
| 1.7 | R9/R10/R11 | R9 经销商拿货（签收第 10 天预警，15 天 deadline）；R10 回访（成交后 15/30/90 天，与 R5 沉默唤醒互斥——已成交客户不走 R5）；R11 阶段停滞（比价>14 天/决策>21 天，纳入统一去重压制） |
| 1.7a | 设备级周期提醒 | 按交付日期起算：电池健康/更换（锂电 3-5 年）/ 轮子磨损（PU/尼龙轮 6-12 个月）/ 液压起升系统检查 / 改装件质保到期 / 以旧换新信号。**注意：本产品为非特种设备（电动托盘搬运车、手动改电动），无年检类提醒** |
| 1.7b | R12 经销商拿货周期预警 | 简单版：经销商 60 天未拿货 → 提醒主动联系；学习版（按历史拿货间隔个性化）进 Phase 4 |
| 1.8 | 成交登记 + 报价版本 | 成交额/型号/数量/交期落字段；报价带版本/生效期/证据链（字段级规格见 5.1） |
| 1.9 | owner + 离职移交 | 本地直改 + 批量移交（复用线索移交逻辑） |
| 1.10 | local_outbox | 只记录不发送，含 event_seq / idempotency_key |
| 1.11 | Action Policy 最小版 | priority + dedup 两字段，R 系列统一接入 |
| 1.12 | 对外文档生成（已修复，待打包验证） | 合同 / 报价 / 开票申请按公司真实样板生成。打包态模板路径 bug 已于 2026-09-02 修复：正确加载 resources/crm-templates 真实模板，移除占位模板静默兜底，模板缺失响亮报错。DoD：打包版用真实记录生成三种文档各 1 份，与样板逐项比对一致 |

### 验收标准（DoD）

1. 备份可恢复：随机抽一周备份完整恢复到新机器，数据校验一致
2. 存量迁移成功率 ≥99%，失败逐条有原因
3. 分配全链路在真实数据跑通 ≥20 条：录入 → 分配 → 销售机可见 → 加好友检测 → 首触
4. 回收误判率 ≤5%（抽查回收案例）
5. R9/R10/R11 各产生过真实卡片且无误报投诉
6. 本地身份档案：每台销售机建档完成，分配认领/审批确认/审计记录中的 actor 正确显示姓名 + 角色（非 wxid）

### 5.1 成交登记与报价版本 · 字段级规格

**成交额（多币种）**
- 本位币 = CNY，成交登记以 RMB 结算额为准（业绩/报表统一口径）
- 字段：`amount_cny`（结算额）+ `original_currency`（默认 CNY，手动下拉选）+ `original_amount` + `rate_note`（汇率备注/凭证，如微信支付订单号、回单截图哈希）
- 外贸单以 RMB 结算，外币仅作报价留痕，**不做自动识别与自动换算**

**型号（双层结构）**
- 主型号：强制从产品库主数据下拉选择，禁止手填（打通知识库，支撑复购归因/售后）
- 扩展属性：自由文本（如"含蓝牙模块 V2.1"）；预设标签集后置到 Phase 4 复购信号启用时再做

**数量（双层）**
- `order_qty`（订单数量）：成交时录入，用于业绩核算
- `shipped_qty`（实发数量）：签收环节手动补录（复用物流认领界面加字段）
- 二者差异 >0 自动触发售后跟进任务（接 R9）；扫码签收不做

**交期（区间型）**
- `expected_ship_start / expected_ship_end`，非单一时点
- 聊天中"下周""月底前"等模糊表达：复用 Hermes 档案更新提案管线（AI 抽区间 → proposed → 销售确认生效），不新建 NLP 模块

**交付与设备**
- `delivery_date`（交付日期）：设备级周期提醒（1.7a）的起算基准
- `opportunity.type`：整车 / 改装（手动改电动是独立生意类型）
- 客户现有设备档案：品牌/车龄/是否已改装——改装潜力与以旧换新信号的来源

**报价版本（一单一版）**
- 每笔成交绑定唯一 `quote_version_id`；append-only，历史版本只读不可改
- 版本关联完整证据链：报价单 PDF 哈希（系统 docxtemplater 生成）+ 聊天截图时间戳 + 销售确认操作日志
- 审计事件全链留痕

---

## 6. Phase 2 · Hermes 与知识治理（约 3~4 周）

### 需求项

| # | 需求 | 要点 |
|---|---|---|
| 2.1 | Hermes MVP | 只做三件：带引用知识问答 / 知识提案 / 确认队列。三段式：触发层 → 工具层（Tool Gateway）→ LLM（aiApiClient）→ 人落锤 |
| 2.2 | 权限链 | User → RBAC → ACL → Tool Permission → Risk → Policy Engine → 执行/审批；高风险（改阶段/移交/发布/对外）必须人工确认 |
| 2.3 | 知识治理 | status / authority / version / TTL + published 过滤 + 审核 Tab + rejected 状态（拒因留档反哺优化）；AI 无 publish 权限 |
| 2.4 | AI 首次分类 | 认领满 24h 触发 + 手动按钮；复用现有分类器/洞察服务；proposed 本机暂存，confirmed 后才可上行。**间接信号提取**：客户不自报家门时，从聊天/昵称/朋友圈/收货地址提取信号（问返点→疑似经销商；"我们冷库"→冷链），带证据出 B 类提案；**信息缺口反问卡**：关键字段缺失时生成行动卡建议销售下次聊天自然提问；证据不足留 unknown（合法状态，不瞎填） |
| 2.5 | 档案持续更新 | 触发：阶段变化/新报价/成交/回访完成/沉默超阈值 → AI 更新提案 → 确认 → 版本+1 |
| 2.6 | 采用率埋点 | 与 Hermes 同天上：Proposal/Knowledge/Action 三类漏斗（generated→viewed→accepted\|modified\|rejected→expired） |
| 2.7 | 冷启动内容包 | 现有话术/FAQ/产品参数官方资料优先；现有 219 条（产品 158 含裸车价 / 话术 59 / FAQ 2，2026-07-29 导入后零增长、无来源与时效字段）只作候选素材，默认 authority=community + staging 待审，人工审核通过才发布；价格类条目与产品库主数据对账，冲突以产品库为准。现有本机知识库不删除、迁移前照常使用；治理版上线后本机库保留为中央主机离线兜底 |
| 2.8 | 审核队列 UX | 批量确认 / 只看 diff / 证据一键跳转；高频 B 类字段支持批量通过 |
| 2.9 | 效果回流 + TTL 巡检 | 知识使用 × 阶段结果归因；知识到期主动问负责人 |
| 2.10 | 商机判定评测（前置门槛） | 现状商机仅 5 条、判定准确率未知。先建标注评测集（≥100 条历史对话，人工标注有无商机与依据片段），基线 P / R 达标后才上线或扩展判定场景；评测集随 Phase 0 evidence 结构落库，可复用现有商机记录回溯标注 |
| 2.11 | Hermes 微信推送（单向起步） | Hermes 结果可推送到微信：客户情况 / 资源分配情况 / 行动摘要。通道选官方单向接口（公众号模板消息或企业微信应用消息），无封号风险；复杂操作点链接回 WeFlow 应用内完成。双向对话 bot 涉及微信自动化（模拟收发），封号风险不可控，本期不做、后置专项评审 |

### 验收标准（DoD）

1. 试点销售（1-2 人）真实使用 2 周
2. 行动卡执行率 ≥60%；B 类确认率 ≥50%（低于则触发批量/阈值优化，不加功能）
3. 越权测试：销售查不到他人客户/成本库 = 0 事故
4. Governance SLA：先纯观测 2 周出 P50/P90，再定阈值（不拍脑袋）
5. Go/No-Go 评审：达标 → Phase 3；不达标 → 停用 Hermes 修产品，Phase 0/1 资产保留
6. 2.10 评测集完成标注并出基线报告；2.11 推送通道在真实手机收到消息

### 6.1 当前实施边界（2026-09-14）

本节只校准“已有代码”和“仍需验收”的边界，不改变上述 DoD：

| 范围 | 当前代码状态 | 尚未完成 |
|---|---|---|
| Phase 0 数据与判断链 | canonical stage、Evidence Resolver、AI Judgment Persistence、Current View、CustomerEvent 与 Action Funnel 已落地并有封板护栏 | 业务裁决最终签字及真实数据持续观察 |
| Phase 1 单机可靠性 | 本地身份、迁移、分配/SLA/移交、SMB 同步、自动备份、售后规则、报价/合同能力已落地 | 新机恢复、真实分配样本、真实售后卡、打包态三类文档及 Windows 等 DoD |
| Phase 2 Hermes | 本机 Tool Gateway 已有 9 个只读工具；知识治理、带引用问答、知识提案、首次分类、采用率基础与 TTL 巡检已有实现 | 官方微信单向推送、≥100 条商机评测、219 条知识人工治理、2 周试点与 Go/No-Go |

这里的 Tool Gateway 是 Electron/Utility 内部白名单，不等同于 Phase 3b 对外部署的 MCP 服务；两者的命名、认证与网络边界分别维护，禁止混为同一接口。

---

## 7. Phase 3 · 企业层（3a 先行 / 3b 后行）

### 7.1 跨阶段继承约束（防止污染 P0/P1/P2）

Phase 3/4 是在现有业务契约上增加中央传输、服务端权限与经营能力，不重做本机事实层：

| 领域 | 必须继承的现有真源 | Phase 3/4 约束 |
|---|---|---|
| 客户阶段 | `customer_profile.stage` + canonical state | 沿用现有枚举和合法写者；中央修正不得成为绕过状态机的万能写入者 |
| AI 当前判断 | `customer_judgment` + `getCustomerCurrentView()` | 双存档沿用判断类型、证据与版本；UI 不得直接拼本地/中央表 |
| 证据 | `shared/messageKey.ts` + Evidence Resolver | 聊天原文不上中央；不得另造 evidence ID 体系 |
| 行动与事件 | `follow_up_task` + 五类型 `customer_event` | 同步元数据不塞入业务事件表；新增事件类型须先过 Feature Gate 并入宪 |
| 分配与归属 | `assignment` + `ownership_history` | 中央计时裁决复用现有状态机、幂等键和 append-only 流水 |
| 同步 | `outbox_event` + Phase 1 ACK/重放语义 | SMB → HTTP 只替换传输 adapter；不得重建第二套 seq/idempotency/ACK 语义 |
| 身份 | Phase 1 本地身份档案 | 企业账号平滑绑定原姓名/角色；本地自报角色绝不升级为服务端权限依据 |
| 知识 | `kbValidEntries` 的 published/TTL/当前版本规则 | WeKnora/Gateway 必须保持同等或更严格过滤，staging/过期/历史版本不得进入 AI |
| 售后复购 | 既有交付售后服务 + `shared/crmRepeat.ts` | Phase 4 只做工单、学习与经营深化，不重写基础生命周期和复购等级 |

### Phase 3a · 同步通道与客户档案库

- 中央主机 PostgreSQL 上线：客户档案/成交/报价/归属/审计
- 双向同步：上行（成交/报价/confirmed 档案/提案/审计）；下行（归属/移交/锁定/企业标签/主管修正/权限）
- **AI 判断 / 画像双存档**：AI 分类、画像、商机判定等 AI 产出，本机存档一份 + 中央主机存档一份；上行内容含判断依据与版本号，便于回溯与离线查阅
- **下行约束**：主管修正不静默覆盖（本地确认）；**离职移交例外**——服务端权限回收优先于本地确认（人走了没人点）
- 本阶段才设计：Offline Sync / aggregate_version / 冲突裁决 / 幂等消费
- **中央主机工作区绑定认证**：首次连接主机输入员工账号（或管理员发邀请码/扫码绑定），设备凭证存系统钥匙串，之后自动登录——销售日常无"登录"感知；主管/分配员/管理员绑定时多一步验证；AI service account 永不人工登录。本地身份档案（1.2a）字段平滑升级
- 离线可用：中央主机非实时依赖

**DoD**：双机同步演练（改→断网→改→恢复→无丢无误）；离职移交全流程演练通过；档案上行延迟 ≤5 分钟。

#### 3a 实施状态（2026-09-15 校准）

本节区分「已实现且自动化验证」与「需部署或真机才能完成」，**不得把前者写成后者已完成**。端点契约见 `docs/API-CONTRACT.md` §3，对象入宪见 `docs/DATA-CONSTITUTION.md` §3.1，审计报告见 `docs/audit/中央同步-阻断项修复-审计报告-claude-20260915.md`。

**已实现且已自动化验证（代码侧）**

- 中央服务（Fastify 5 + PostgreSQL + Docker Compose）与 11 个端点（健康/就绪、邀请码、认领、轮换、自助解绑、管理员吊销、推送、拉取、回执、下发指令）；设备令牌与邀请码**只存 sha256 哈希**。
- 五角色服务端权限矩阵（sales / supervisor / allocator / admin / service）+ 工作区隔离 + 员工/设备定向；**本机自报角色仅署名，不作鉴权依据**。
- **出机字段边界**：每个上行 `eventType` 走**显式最小字段白名单**（不整包投递 outbox 原始载荷）；身份值只上行 sha256 哈希 + 展示掩码（哈希复用既有身份归一规则）；`session_id`、聊天正文与客户原话永不出本机；服务端对投影载荷**严格白名单，多字段即拒收**；拦截与擦洗**只记字段路径与错误码，不记值**。
- **工作区内的设备命名空间与归属**：上行 `entityId` 必须属本设备命名空间；既有投影只允许**原 `source_device_id`** 更新（跨设备改写显式冲突，更高 `aggregateVersion` 亦不覆盖）；唯一身份冲突落冲突记录；`sales` 只能上传销售设备可合法产出的类别；**bootstrap 管理员无工作区上下文时不得调用常规同步接口**。
- 上行：复用 `outbox_event`；10 类实体落**显式投影表**（禁止通用 JSONB 数据桶）；可变表按 **(updated_at, id) 复合水位**增量，幂等键带实体版本（新版本 → 幂等键不同、`entityId` 稳定、`aggregateVersion` 严格递增）；被过滤行不阻塞后续合法行，补齐后经台账重扫。
- 下行：`assign`/`transfer`/`recycle` 以**命令**下发并复用 Phase 1 既有状态机与幂等标记（只换传输 adapter）；指令契约唯一真源 = `shared/centralDownCommand.ts`（状态机 SMB 与 HTTP 共用——SMB 入口 `validateDownEventFile` 已真正调用 `validateDownCommand(subject,'smb')`，仅 `payload.lead` 字段集按传输上下文分档：中央 HTTP 6 字段 / SMB 8 字段），`eventType`/`entityType` 不匹配拒收、畸形目标返 400；`supervisor_correction` 落 `notify_inbox` **不静默覆盖**；`permission_change` 只记声明；`sla1_escalate_supervisor` 按 `centralSyncSupervisorCode` **稳定工号**定目标（**绝不按姓名猜人**）；未知类型立即 `invalid`，`nolead` 有界重试后 `invalid`（**不无限 retry**）。**建档契约**：`assign`/`transfer` 的 `lead` 必须含最小身份（`leadId` 正整数 / `contactType` 枚举 / `contactNormalized` 非空），中央 HTTP 固定 6 字段全部存在，顶层与子对象 `leadId` 必须一致。**`transfer` 一条 outbox → 两条指令**（接收方 `apply` / 原归属 `remove`，幂等键各带投递角色与目标员工），**两个目标都被中央受理才结算 `sent`**，任一目标 4xx 整行 `failed`（审计带 `failedRole`/`failedTarget`/`delivered` 供人工修复）、网络类失败保持 `pending` 靠顺序重试收敛（已受理目标由中央幂等去重，不另建发送状态表）。**`transfer` 的 `sla1Deadline`/`mode` 必填**：sla1Deadline 为发起端移交事实产生时确定的有限正整数时间戳，接收端精确按指令值落地（`assignment.sla1_deadline` = `lead.first_contact_deadline`），**不在接收端重算**；`remove` 不建行也不动 SLA；重放零漂移。
- 绑定/解绑生命周期与调度器：绑定必启、解绑安全空转、轮巡间隔实时生效、不产生重复定时器；解绑**先请服务端吊销再清本地**，失败时如实报「解绑未完成」，不假装已撤销；**只有中央确认接收后才结算本机 outbox 行**。
- 传输互斥：`centralSyncEnabled` 为真时 Phase 1 SMB 同步自动停用；关闭时 Phase 1 行为完全不变。
- **服务端实体引用闸门**：`/sync/push` 逐条校验实体引用类别（`validateCentralEntityId`），并对 `payload` 内已登记的 `*Ref`（`customerRef`/`leadRef`/`opportunityRef`/`employeeRef`）校验形态、类别与命名空间归属，**借用同工作区他机命名空间同样拒绝**；拒收只影响该条事件，同批合法事件照常落库，被拒事件不留 `sync_event`、不留投影、不消耗幂等键。
- **中央自身操作审计**（`central_audit_event`，append-only，与只读上行投影 `central_audit_projection` 严格分离）：邀请码签发（与签发**同一事务**，不记邀请码明文/哈希）与下行指令**首次**受理（只记 `eventId`/`eventType`/投递目标，**不记载荷**）均已落库；**幂等重放不追加审计、被拒请求不留痕**。
- 验证（2026-09-15 实测）：`scripts/central-sync-client-test.ts` 24/0、`scripts/central-sync-adapter-test.ts` **86/0**、`scripts/central-sync-e2e-test.ts` **61/0**（真实生产者 → outbox → 适配器 → Fastify `app.inject` → 内存中央库 → 投影/指令 → pull → 本机状态机 → ACK → 结算的完整契约闭环；**J 段为真实移交链**——调用真实 `transferAssignment()` → 一条 outbox → 两条下行指令（`apply`/`remove`）→ 双目标都受理才 `sent` → 重放判 duplicate → 第二目标先瞬时失败保持 `pending`、恢复后补齐 → 两个接收端各经既有状态机落地并回 ACK；**无 Docker 依赖**）；`central/` 侧 app **136** / projection **36** / migration 23 / context 6（全 0 失败），`npm run typecheck` 与 `npm run build` 通过。`central/test/app-test.ts` 跑在 **MemoryCentralStore** 上，**不能替代真实 PostgreSQL 验证**。

**已披露残留（不声称已解决）**

- **下行指令的线索档案面**：只有 `assign`/`transfer` 携带 `lead` 子对象（`recycle` 与通知/声明类指令不带，携带即整事件拒收）。中央 HTTP 通道固定 6 个字段（`CENTRAL_LEAD_FIELDS`：`leadId`/`name`/`contactType`/`contactNormalized`/`source`/`note`），`contactRaw`/`wechat` 一律 400 且不落库；SMB 内网文件通道保留 Phase 1 的 8 字段历史口径（已互信局域网，未被收窄）。因接收端 Phase 1 状态机按 `(contact_type, contact_normalized)` 定位/创建线索，`contactNormalized` **必然过网**；聊天正文两个方向都拦，此面为有限披露例外，**不声称「下行零身份值」**。
- **冲突裁决**：仍为「服务端版本闸门 + 跨设备改写拒绝 + 唯一身份冲突记录 + 客户端 `conflict` 回执」，多写者合并策略未定。

**已实现但待部署验收（代码就绪，需真实环境）**

- `docker build` 与镜像体积；真实 PostgreSQL 实例上的迁移与端到端同步；反向代理 / 证书 / HTTPS 真实终结。
- 双机同步演练（改→断网→改→恢复→无丢无误）；离职移交全流程演练；档案上行延迟 ≤5 分钟实测。
- Windows 打包态下的绑定/同步表现；SSE 与真实微信消息推送验收。

**未实现（属 Phase 3b 或后续）**

- WeKnora 知识底座与中央 MCP 工具面（§4.2 仍为占位规范）。
- 官方微信单向推送（2.11 未完成）、≥100 条商机评测集（2.10 未完成）——与中央节点无关的独立遗留项。
- Phase 4（经营深化）**为未启动的 Backlog**，本轮未触碰。

### Phase 3b · 知识底座与权限

- WeKnora 部署（Docker 5 核心容器 + MCP 单独起）；staging/published 双库隔离
- 模型：DeepSeek + bge-m3 + bge-reranker
- 客户档案检索副本：confirmed 档案单向投影、副本只读、owner_sales 标签 + Gateway 条目级 ACL、敏感字段脱敏、移交/作废同步失效
- **DoD**：100 条评测集（含 15 条时效陷阱/10 条权限/5 条拒答）答案正确率 ≥85%、过期命中=0、越权=0、档案越权检索=0

---

## 8. Phase 4 · 经营深化

**本阶段不设时间表，作为 Backlog 管理**，按试点呼声排序启动：目标管理 / 售后工单+复购信号 / 沉默客户前兆 / 竞对情报 / 知识缺口清单 / 公海（统一回收引擎）/ 知识运营看板 / 多渠道录入 / **经销商维护深化**（动销关怀"卖得怎样、库存几台" / 价格政策同步任务 / 年度协议返利到期提醒 / R12 拿货周期学习版）。

**与 Phase 1 已实现能力的边界**：基础售后生命周期、交付登记、设备档案、数量差异/质保/以旧换新提醒、复购等级，以及 R9/R10/R11 和 R12 固定 60 天简单版均为现有基线，Phase 4 不重复建设。这里的“售后工单+复购信号”指正式工单状态机/SLA、复购信号深化；“经销商维护深化”仅指动销库存、价格政策、年度协议返利和个性化拿货周期学习。

**产品口径备忘**：本项目产品为电动托盘搬运车、手动搬运车改电动（非特种设备），无年检类合规提醒；PRD 与评测集中的产品示例（型号/载重/电池/续航/通道宽度）在 Phase 0 定稿前替换为真实产品清单（待业务方提供）。

启动任一 Backlog 项须过 Feature Gate 七问。

---

## 9. 数据迁移方案（Phase 1 第 1 周执行）

| 数据源 | 目标 | 规则 |
|---|---|---|
| 现有 customer_profile / account | customer + ownership | owner 空 → 归销售本人，留迁移标记 |
| 现有线索（含群扫描） | lead → customer_identity | 手机号/wxid 查重归并 |
| 待导入存量线索（含奋斗群批量约 4,281 条） | 资源池 | 走分配流程，不直接落归属；先 200 条小批量验证（见 1.3） |
| 历史成交记录 | opportunity（won）+ 成交登记 | 补 quote 首版本 |
| 现有知识库三类条目（219 条） | 治理版知识库 staging 区 | 默认 authority=community、version=1，**人工审核通过才发布**（与 2.7 一致）；价格类与产品库主数据对账，冲突以产品库为准；重新确认 TTL |

迁移产出：迁移报告（总数/成功/失败/冲突清单），失败条目人工处理窗口 1 周。

---

## 10. 风险登记册

| # | 风险 | 等级 | 对策 |
|---|---|---|---|
| R1 | 微信版本升级致解密失效 | 🔴 数据底座单点 | 降级预案：解密失效时 CRM/历史数据照常可用，仅无新聊天流入；告警 + 跟进上游适配 |
| R2 | 销售不确认 / 主管不审核 | 🔴 人的 P0 | 采用率埋点前置、批量确认、冷启动内容包、Go/No-Go 门 |
| R3 | 中央主机是 SPOF | 🔴（部署验收前） | 四项上线保护：① 双盘镜像与两盘独立启动验收；② UPS 断电关机与来电恢复；③ 一致性加密备份、异地副本及异机恢复；④ 本机监测与独立外部心跳告警。任一未验收保持 🔴；全部通过后可降为 🟡，整机故障仍需恢复。RPO/RTO 由采购方案 §3.2 明确并演练。 |
| R4 | PIPL 合规 | 🟡 | 聊天原文不出本机；上行字段脱敏清单（成本/手机号打码范围）随 Phase 3a 评审 |
| R5 | 中央节点无人运维 | 🟡 | 下单前落实内部责任人或外部维护服务、备用联系人和响应时限；置于有人可达处，告警到人。无人负责不得上线，换 NAS 不豁免；Phase 0-2 分配服务过渡期跑主管机。 |
| R6 | 提醒疲劳 | 🟡 | Action Policy 去重压制；SLA 阈值从观测数据定 |
| R7 | 多微信号归属混乱 | 🟡 | customer_identity 跨号归并 + 合并审批 |
| R8 | 微信自动化封号风险 | 🔴 账号资产 | 只走官方单向推送通道（公众号模板消息 / 企业微信应用消息）；不做消息模拟收发、不做自动加人；双向 bot 后置专项评审 |

---

## 11. ADR-001：弃用 Twenty CRM 底座

**背景**：早期 PRD（团队版）曾选型 Twenty 作为 CRM 底座。

**决定**：弃用 Twenty，企业事实层自研（中央主机 PostgreSQL + 轻量同步）。

**理由**：
1. **数据生产方式不同**——Twenty 是表单驱动 CRM（销售手动填卡）；本项目核心是聊天驱动（AI 从聊天自动建档）。让销售填两遍表等于杀死系统最大卖点。
2. **违反 SSOT**——Twenty 会携带自己的客户/阶段模型，与 WeFlow 本地事实形成第二事实源。
3. **不解决真难点**——本项目难在单机节点 + 离线可用 + 事件同步，Twenty 均不覆盖，引入后这些仍需自研，反而多套系统要维护。

**后果**：企业层需自研（已计入 Phase 3a 工作量）；团队版 PRD 文档归档标注"已被 ADR-001 取代"。

---

## 12. ROI 基线

| 指标 | 现状值（2026-09-02 实测） | 目标值 |
|---|---|---|
| 线索首触完成率 | 1 / 4,680（自领模式未运转，分配制上线后重测） | 分配后 48h 内首触 ≥80% |
| 销售翻聊天记录找信息时长/天 | ？（调研采集） | ↓ 30% |
| 离职销售客户交接完整率 | ？ | 100% |
| 知识复用（话术/FAQ 被引用次数/周） | 0（知识库 219 条零增长、FAQ 仅 2 条） | ≥20 |
| 行动卡执行率 | 0.6%（12,952 张任务卡执行 80 张） | ≥60%（Phase 2 行动卡门槛） |

---

## 13. 里程碑

| 时间 | 里程碑 |
|---|---|
| W1-W2 | Phase 0 宪法冻结（本文档评审通过） |
| W3-W5 | Phase 1：备份先行（W3 内交付）→ 迁移 → 分配 → 售后规则 |
| W6-W7 | 试点（1-2 名销售真实使用） |
| W8-W11 | Phase 2 Hermes MVP + 采用率观测；中央主机基础安装部署已由负责人完成，配置与演练证据待回填 |
| W11 末 | **Go / No-Go 评审** |
| W12+ | Phase 3a/3b（衔接中央主机就位） |
| 持续 | Phase 4 Backlog 按呼声启动 |

### 13.1 中央节点采购策略（阶段一主机 / 阶段二按需升 NAS）

**阶段一 · 独立主机（当前方案；基础安装部署已完成，Phase 3 业务服务待接入）**

- **形态**：本次基线为 x86-64 商用迷你主机，二手可选；核验具体 SKU、成色、散热与盘位配件，不以体积推断可靠性。
- **规格**：内存 ≥16GB（推荐 32GB）/ Docker / ≥2 个内部存储盘位（双 M.2 或 M.2 + 2.5″）/ 两块等容量 SSD / 千兆起步，2.5G 可选。单盘位不纳入本次采购。
- **四项上线保护**（与 R3 一致，主机与 NAS 均须验收）：① 镜像及单盘独立启动；② UPS 与断电/来电恢复；③ 一致性加密备份、异地副本与异机恢复；④ 本机监测与外部心跳告警。RAID 不替代备份，NAS 也需另配 UPS 并配置保护措施。
- **就位状态**：用户已确认部署负责人完成基础安装部署；D1/D2/D3 视为采购/安装流程已落实。D4/D5、恢复目标以及 RAID/UPS/备份/告警实测仍以采购方案 §9 的证据回填为准，未回填前不得把四项上线保护描述为已验收。

**阶段二 · 视情况升 NAS（非必须）**

- **触发条件**：需要热插拔、厂商支持或更易维护的存储管理；盘位不足或实测资源/恢复目标不达标时，比较扩容、换主机与 NAS。人数增长只触发复评，不直接决定采购。
- **运维条件**：始终须有内部责任人或外部维护服务。无人运维不得上线，不能通过换 NAS 豁免应用、数据库和恢复责任。
- **迁移边界**：主机 → NAS 保留已有 API/事件契约，通常只换部署与连接地址；Phase 1 SMB → Phase 3a 内网 API 是独立的软件演进，权限、冲突裁决与同步范围仍按 Phase 3a 实施。
- **迁移路径**：预演 → 旧节点停写并冻结后台任务 → 最终一致性备份/恢复（含文件、队列与去重状态）→ 校验 → 隔离旧写者后切换 → 积压重放验证。新节点开放写入后，回退须先停写并逆向恢复最新数据，禁止直接切回旧库。详见采购方案 §5。
- **架构**：arm64 为替代方案，须验证锁定版本的全部容器与数据库扩展；不能由主应用镜像支持情况推断整栈兼容。

**实施细节**（机型 / 盘位 / UPS / 备份拓扑 / 上线检查单）见 [中央节点采购方案](中央节点采购方案.md)。

---

## 14. 铁律（继承并追加）

继承现有：消息读取只走 chatService；AI 调用只走 aiApiClient；后台任务走 enqueueSalesTask；tsc 零错误才提交；WCDB 时间戳是秒。

追加：SSOT（一个事实一个源）；AI 无 publish 权；Feature Gate 七问；不静默覆盖本地数据；审计不可删。
