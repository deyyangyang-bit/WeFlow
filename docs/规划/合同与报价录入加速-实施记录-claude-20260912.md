# 合同与报价录入加速 —— 实施记录（2026-09-12，工作区未提交）

> **实施依据**：`合同与报价录入加速-PRD-v1.4`（§10 验收标准 / §11 测试计划为权威）。
> ⚠️ 该 PRD 文件在工作区已被**另一任务**删除（可能因含真实客户样本）。按任务约束**未恢复该路径**，本次实施与自查一律以取出的 HEAD 版 `合同与报价录入加速-PRD-v1.4-HEAD版.md`（941 行）为准；工作区另有该任务的 v1.5 版与《合同抬头样本分布.md》，本记录不主张其改动。
> **数据 SSOT**：`docs/DATA-CONSTITUTION.md`（本刀新增两条登记，见 §6）。
> **状态**：代码与文档改动**全部留在工作区，未 commit、未 push**（任务约束）。
> **范围**：PRD §7.2 抬头解析 P1 主体 + §11.1 缺失的自动化测试 + §10 逐条自查；后端与页面主体（录入链幂等、报价单价覆盖、差异确认、草稿生命周期、文件结果条）为 HEAD 既有，本刀复核确认（见 §0、§1.3）。

---

## 0. 开工前核对（依据来源、工作区状态、落地分层）

**工作区状态**：`git status` 如实核对，工作区存在**另一任务（隐私清理重构）**的大量未提交改动（123 个改动文件，含 `shared/safeJson.ts`、`electron/utils/numberUtils.ts`、`electron/services/salesMessageText.ts` 等新增与多处组件/服务改写）。**未回退、未提交、未顺手清理**；本刀改动叠加其上。少数文件（`CrmWorkbenchPage.tsx`、`main.ts`、`HANDOVER.md` 等）同时承载两个任务的改动，**本记录只主张属于本刀的部分**。

**落地分层**（重要，避免把 HEAD 既有当成本刀成果，也避免漏认本刀缺口）：

| 层 | 内容 | 本刀动作 |
|---|---|---|
| HEAD 已落地并提交（`53f503c` 等） | 后端录入链：`beginContractEntry`（按 `creation_request_id` 幂等）/ `contractByCreationRequest` / `createQuotation` 单价覆盖 + `quote_version_create.detail.price_overrides` / 差异确认（`updateHeaderKeys` 勾选语义）/ 草稿生命周期 / `GeneratedFileResult` 结果条 / 金额与报价合计的关系 | **复核 + 补测试 + 登记文档**（§1.3、§2） |
| 本刀新增（工作区未提交） | `shared/buyerHeader.ts` 抬头解析器 + 14 例脱敏 fixture + 页面粘贴接线 + 69→76 条单测；`crmDocGenCore.reuseExistingDocPath`；`aiDailyLimitAudit.ts`（属 AI 简报遗留项，见另一份实施记录） | **实现 + 测试**（§1.1、§1.2） |
| HEAD 未做（本刀闭合的真实缺口） | §11.1 要求的自动化测试**在 HEAD 上一项都不存在**（全仓 `grep price_overrides` / `creation_request_id` 在 `scripts/*.ts` 中零命中，尽管落地提交声称覆盖） | 补 `crm-workbench-test` 第 13 节 15 条 + `buyer-header-test` 全量 + docgen 复用判定 3 条 |

---

## 1. 改动清单

### 1.1 本刀新增文件

| 文件 | 要点 |
|---|---|
| `shared/buyerHeader.ts` | 抬头解析**纯确定性规则**（§7.2 全文）：别名表整行精确匹配、组合标签只识别不拆分、值内空白按字段区分、标签与值分行/冒号后为空读下一行/下一行是标签则放弃、每字段只取第一个非空值、未识别行跳过不报错。导出 `parseBuyerHeader` / `recognizedFieldCount` / `BUYER_HEADER_FIELD_LABELS` / **`buyerHeaderOutcome`**（失败判定与两种失败提示收在共享层，调用方不自行约定——§7.2.4 第 14 条 / §10.1 最后一条） |
| `scripts/fixtures/buyer-header-real.txt` | 14 组脱敏样本用例（含 `expected` 段）。覆盖《合同抬头样本分布》§3.3 全部异常形态与 §4.3 全部负面标签：标准六行、缺电话（+`项目名称` 承载地址不命中）、含联行号（跳过）、组合标签、仅命中组合标签、账号在开户行前（乱序）、标签与值分行、全角冒号与标签含空格（`税　号`）、值内分段与尾随空格、地址含后续冒号、重复字段取首个非空值、`名称` 命中与 `产品名称` 不命中（+`数字钱包账号`）、标签与值都是标签时放弃、无冒号噪音行。**全部数值为合成值**，真实税号/账号/电话/地址不入库 |
| `scripts/buyer-header-test.ts` | 76 条断言：fixture 驱动全用例、成对命中/不命中、值内空白按字段、分行取值、组合标签四项不写入、两种失败可区分、边界不抛异常、全角冒号/乱序/重复、地址内冒号、**冻结别名表逐条命中 + 6 个负面标签一律不命中**、页面接线静态断言 |

### 1.2 本刀修改文件

| 文件 | 要点 |
|---|---|
| `src/pages/CrmWorkbenchPage.tsx` | 两处粘贴入口接线（新建合同 / 已建合同详情）：`headerPaste(...)` 渲染助手（`textarea` + `onPaste` 立即解析 + 「重新识别」「清空原文」）、`applyNewHeader` / `applyEditHeader` 回填、提示按 §8 语义三档配色与 `role`（失败 `alert`、其余 `status`）、换客户/换合同/重置时清掉粘贴原文与旧提示；失败路径改为消费 `buyerHeaderOutcome`（两处入口不再各抄一份文案） |
| `src/pages/CrmWorkbenchPage.scss` | `.header-paste*` 样式（textarea 走 `--color-border`/`--color-bg-surface`/`--radius-control`，成功浅蓝 / 待处理浅黄 / 未识别浅红三档 token 配色，无硬编码 hex） |
| `scripts/crm-workbench-test.ts` | 新增第 13 节 15 条（74 → **89**）：`isPriceOverride` 边界；未改价不写 `price_overrides`；显式同价不写；改价 detail 精确三元组；**页面「已改价」条数 == 审计 `price_overrides` 条数同源断言**；`contractByCreationRequest` 命中/未命中/标识非法；幂等重试（报价 + `beginContractEntry` 重放）；空档案直接写入；部分 `updateHeaderKeys`；合同快照保留未勾选值；空 `updateHeaderKeys` 不更新；草稿解析静态断言；预校验先于 IPC 静态断言 |
| `electron/services/crmDocGenCore.ts` | 新增纯函数 `reuseExistingDocPath(attachmentPath, fileExists)`（§10.4 [P2]：已有产物且文件仍在 → 直接复用路径，不重复生成）。判定与 fs 探测分离，便于穷举断言 |
| `electron/services/crmDocGenService.ts` | `reuseExisting` 分支改走该纯函数（语义不变：非空路径 + 文件存在才复用） |
| `scripts/crm-docgen-test.ts` | 新增 3 条复用判定断言（78 → **81**） |
| `docs/DATA-CONSTITUTION.md` / `docs/API-CONTRACT.md` / `docs/HANDOVER.md` | 见 §6 |

### 1.3 本刀复核确认（HEAD 既有，未改动）

| 行为 | 位置 |
|---|---|
| 录入链作用域指纹与账号切换防御 | `crmDbService.contractEntryScope` / `assertContractEntryScope`；页面 `entryScopeRef` + `entryEpoch` 在每次 IPC 后 `checkScope()` |
| `creation_request_id` 随合同长期保留，「清除」只丢本地草稿 | `contract.custom_fields.creation_request_id`（DATA-CONSTITUTION §2026-09-12 登记）；`persistDraft`/`discardDraft` 只动 localStorage |
| 防重复的权威防线是**数据库查询** | 刷新后 `contractByCreationRequest` 判定草稿指向的合同是否已完成，再决定恢复或重新生成标识 |
| 逐行改价走后端既有报价单价覆盖能力 | `createQuotation` → `createQuotationVersionTx`，`price_overrides` 与版本同事务（宪法 §1.6 修订） |
| 创建链部分成功不得静默 | `partial_quotation_failed` / `partial_document_failed` 两态 + 明确文案「合同已创建，但…」+ 按钮改文案为「重新创建报价单」/「重新生成合同」 |
| 生成成功后提供「打开」与「打开所在文件夹」 | `GeneratedFileResult`（工作台 / 确认中心 / 商机页三处复用），`openPath` 非空错误与 `showItemInFolder` 失败均显示 |

---

## 2. PRD §10 验收清单逐条自查

图例：✅ 通过（有可复现证据） / 🟡 部分通过（后端或静态接线有证据，**UI 未实测**） / ⬜ 无法验证 / ❌ 未通过。

### 10.1 抬头解析（16 条）

| # | 验收项 | 结论 | 依据 |
|---|---|---|---|
| 1 | 正式编码前已收集 10–20 份真实样本（Gate 0 通过） | ✅ | 《合同抬头样本分布》§2：11 份 ✅、组合标签统计 ✅、别名表评审冻结 ✅、原始样本仅本地 ✅（该文档属另一任务改动面，本刀仅引用不主张） |
| 2 | 组合标签已单独统计，别名表有对应评审记录 | ✅ | 同上 §3.3（1/11，远低于拆分阈值）+ §4.1 冻结表；`buyer-header-test` 11a 逐条锁定 6 字段 24 个标签的命中、11b 锁定 6 个负面标签不命中——**别名表是契约，改动必须同步测试** |
| 3 | 含 `税    号`、`帐号`、尾随空格时六字段正确识别 | ✅ | 测试 3a–3e、8a；fixture「全角冒号与标签含空格」「值内分段与尾随空格」；`帐号` 由 11a 覆盖 |
| 4 | 值内分段规范化（税号/账号/电话去空白；地址/开户行保留） | ✅ | 测试 3a–3e（含 Tab 与全角空格）；fixture 同例 |
| 5 | 支持字段乱序、缺项、全角/半角冒号混用 | ✅ | 测试 8a；fixture「账号在开户行前」「缺电话」 |
| 6 | 支持标签和值分两行 | ✅ | 测试 4a–4c；fixture「标签与值分行」 |
| 7 | 地址中后续冒号不被截断 | ✅ | 测试 9a；fixture「地址含后续冒号」 |
| 8 | 值中的「号」字不影响匹配 | ✅ | 测试 9a（值含「号」）；匹配只对标签侧做整行精确比较 |
| 9 | 空文本和纯噪音返回空结果且不抛异常 | ✅ | 测试 7a（空串/纯空白/噪言行）、7b（非字符串） |
| 10 | 「名称」正确识别为单位名称 | ✅ | 测试 2a；fixture「名称命中与产品名称不命中」 |
| 11 | 「产品名称」不识别为单位名称 | ✅ | 测试 2b；11b 负面标签 |
| 12 | 重复字段只取第一个非空值 | ✅ | 测试 8b；fixture「重复字段只取第一个非空值」 |
| 13 | 组合标签被检出并提示手工拆分，系统不猜测写入 | ✅ | 测试 5a–5d；页面提示「检测到合并字段，请手工拆分后确认：{原文}（覆盖 …）」（12f 断言） |
| 14 | 组合标签命中时地址、电话、开户行、账号四项均不被写入 | ✅ | 测试 5a（四字段全 undefined）；fixture「组合标签开户行及账号」 |
| 15 | 仅命中组合标签时判定失败，提示同时含未识别与待拆分 | ✅ | 测试 6d（`failure: 'combo_only'` + 提示原文） |
| 16 | 返回结构能区分两种失败，不需要调用方自行判断 | ✅ | **本刀补齐**：`buyerHeaderOutcome` 返回 `{ ok, failure: 'unrecognized' \| 'combo_only', message }`（测试 6c–6f）；两处入口只做 `text: outcome.message` 透传，页面内**已无失败文案字面量**（12f 反向断言） |

### 10.2 客户边界（14 条）

| # | 验收项 | 结论 | 依据 |
|---|---|---|---|
| 1 | `[P0]` 选客户 A 后切新建客户，名称与五项抬头全部清空 | 🟡 | 代码路径明确（[CrmWorkbenchPage.tsx:566-584](src/pages/CrmWorkbenchPage.tsx#L566-L584) 的 `else` 分支清名称+五项，并清粘贴原文与提示）；**属 §11.2 人工 UI 验收，本机未点击实测** |
| 2 | `[P0]` 上述切换不清空合同金额与已选产品 | 🟡 | 同一 handler 未触碰 `newAmount` / `newQuoItems`；未实测 |
| 3 | `[P1]` 未选客户时解析出的单位名称可回填客户名称 | ✅ | 12d 静态断言 + [:428](src/pages/CrmWorkbenchPage.tsx#L428) |
| 4 | `[P1]` 已选客户时解析不覆盖客户名称 | ✅ | `nameIgnored = newAccountId > 0 && !!f.buyerName`，提示「已选客户，单位名称以客户档案为准；」；名称输入框 `readOnly` |
| 5 | `[P1]` 已建合同详情中解析不修改 `account.name` | ✅ | 12e：`applyEditHeader` 只调 `setEdit*`，900 字符窗口内无 `setNewName` / `crm.update('account'` |
| 6 | `[P1]` 新建客户时确认后的五项抬头同步写入档案 | 🟡 | 后端已证（13h 空档案 → 五项全写）；页面 :365 带 `header` 与 `accountId: 0`；**端到端未实测** |
| 7 | `[P1]` 档案抬头五项全空时直接写入且不弹差异确认 | ✅ | [:358](src/pages/CrmWorkbenchPage.tsx#L358) 要求 `headerKeys.some(k => String(accountFields[k]||'').trim())` 才弹；13h |
| 8 | `[P1]` 档案已有抬头且存在差异时才弹差异确认 | ✅ | 同一条件 + `differences.length`；13i |
| 9 | `[P1]` 差异确认在提交创建时弹出，不在粘贴瞬间弹出 | ✅ | 态在 `createContract` 内设置，`applyNewHeader`/`applyEditHeader` 不触碰 `headerChoices` |
| 10 | `[P1]` 差异确认逐项可勾选，只有勾选项写入档案，未勾选保持原值 | ✅ | 勾选框 `onChange` + `updateHeaderKeys` 过滤；13i（部分写入）、13i2（快照保留未勾选值） |
| 11 | `[P1]` 全部取消勾选等同「不更新」 | ✅ | 「不更新客户档案」按钮置 `{}`；13j（空 `updateHeaderKeys` → 不更新） |
| 12 | `[P1]` 给老合同补录抬头不更新客户档案 | ✅ | `saveInvoiceInfo` 只写 `contract.custom_fields`；12e |
| 13 | `[P1]` 同一客户第二份合同默认从档案带出五项 | ✅ | [:572-581](src/pages/CrmWorkbenchPage.tsx#L572-L581) 选客户即回填档案五项 |

### 10.3 报价（12 条）

| # | 验收项 | 结论 | 依据 |
|---|---|---|---|
| 1 | `[P0]` 0 元单价可以提交 | ✅ | `quoteRowError` 判 `unit_price < 0` 拒绝、0 通过（[priceOverride.ts:7-12](shared/priceOverride.ts#L7-L12)）；13a 边界含 0 |
| 2 | `[P0]` 空、负数和非有限单价被阻止 | ✅ | 同行判空 + `Number.isFinite`；13a |
| 3 | `[P0]` 数量空、0、负数或小数被阻止 | ✅ | `Number.isSafeInteger(qty) && qty > 0`；后端 `crm-opportunity-test` 9d/9d2 双保险（0 与 1.5 均拒绝） |
| 4 | `[P1]` 新建合同与已有合同两条路径均显式传 `unit_price` | ✅ | 两处 `rowPayload` 均含 `unit_price`；13b/13c/13d |
| 5 | `[P1]` 搜索列表目录价保持只读 | ✅ | 搜索结果行为纯文本 `¥{unit_price}`，只有「添加」按钮；可编辑的是加入后的「本次单价」 |
| 6 | `[P1]` 未改价时审计 detail 不出现 `price_overrides` | ✅ | 13b（省略 `unit_price` 与显式同价两路） |
| 7 | `[P1]` 改价时审计含准确的产品、目录价、报价价 | ✅ | 13d 断言 detail 三元组（按分比较） |
| 8 | `[P1]` 页面「已改价」条数与审计 `price_overrides` 条数完全一致 | ✅ | 13e：混合一行改价一行原价的报价版本，`pageOverrideCount === auditDetail.price_overrides.length` |
| 9 | `[P1]` 存量迁移调用不传新参数仍通过 | ✅ | 13b/13c（`unit_price` 省略即旧行为）+ 版本链既有测试全绿 |
| 10 | `[P1]` 手工改金额后报价合计变化不覆盖手工值，只提示并提供一键同步 | ✅ | [:344](src/pages/CrmWorkbenchPage.tsx#L344) 手工值优先；[:636](src/pages/CrmWorkbenchPage.tsx#L636) 提示 + 「同步报价合计」按钮 |
| 11 | `[P1]` 清空合同金额后恢复跟随报价合计 | ✅ | `newAmount.trim() === '' → newQuoTotal`；同步按钮即 `setNewAmount('')` |

### 10.4 部分失败（14 条）

| # | 验收项 | 结论 | 依据 |
|---|---|---|---|
| 1 | `[P0]` 报价失败时明确说明合同已创建 | ✅ | [:373](src/pages/CrmWorkbenchPage.tsx#L373)「合同已创建，但报价单创建失败：…」+ 阶段态 `partial_quotation_failed` |
| 2 | `[P0]` 同一次会话内重试报价不创建第二份客户或合同 | ✅ | `requestId` 幂等：13g（报价重试）、13g2（`beginContractEntry` 重放返回同一合同） |
| 3 | `[P0]` 前端预校验失败时不调用后端创建接口 | ✅ | [:343-349](src/pages/CrmWorkbenchPage.tsx#L343-L349) 全部 early return 在 IPC 之前；13l 静态断言 |
| 4 | `[P0]` 重复点击提交不产生重复记录 | ✅ | `creatingRef` / `quoteCreatingRef` 同步置位（第一个 await 前） |
| 5 | `[P1]` 页面刷新后仍能依据数据库恢复已完成阶段、避免重复创建 | ✅ | [:57-71](src/pages/CrmWorkbenchPage.tsx#L57-L71) 草稿 + `contractByCreationRequest`；13f/13g |
| 6 | `[P1]` 已存在目标报价版本时重试跳过报价创建 | ✅ | 13g2 + `createdRef.quotationId` 短路 |
| 7 | `[P1]` 创建链完成后丢弃本地草稿 | ✅ | [:384](src/pages/CrmWorkbenchPage.tsx#L384) `discardDraft()` |
| 8 | `[P1]` 用户取消后丢弃本地草稿 | ✅ | `resetNew()` 首行 `discardDraft()`（「取消」按钮） |
| 9 | `[P1]` 残留草稿指向已完成合同时不复用，重新生成标识 | ✅ | [:60/:74/:76](src/pages/CrmWorkbenchPage.tsx#L60) 判定 complete → `discardDraft()` 后走新标识分支 |
| 10 | `[P1]` 残留草稿指向未完成合同时恢复且不重复创建 | ✅ | [:61-71](src/pages/CrmWorkbenchPage.tsx#L61-L71) 回填 `createdRef` + 明确提示「不会重复创建合同」 |
| 11 | `[P1]` 本地存储被清空后降级为人工确认，不自动复用旧合同 | ✅ | 草稿为 null → 新标识 + `headerConfirmed=false`（有档案差异时仍走人工确认）；写草稿失败也提示「刷新后请先查看合同列表确认，不要重复创建」 |
| 12 | `[P2]` 文档失败时重试不会创建第二个报价版本 | ✅ | 重试按钮只重跑 `docGenerate`（`partial_document_failed` → formLocked），不回到报价创建 |
| 13 | `[P2]` 已存在有效生成文件时重试直接展示文件，不重复生成 | ✅ | **本刀补测**：`reuseExistingDocPath` 三组合断言（docgen 81/0）+ `docGenerate(..., { reuseExisting: true })` |

### 10.5 文件操作（6 条）

| # | 验收项 | 结论 | 依据 |
|---|---|---|---|
| 1 | 合同工作台生成合同和报价后均显示结构化结果条 | ✅ | `artifact` 态 + `GeneratedFileResult`（[:508](src/pages/CrmWorkbenchPage.tsx#L508)） |
| 2 | 确认中心两类文档生成后均可打开和定位 | ✅ | `CrmReviewPage` 两类型独立 artifact 键 |
| 3 | 商机页每个报价版本操作自己的文件 | ✅ | `OpportunityPage` 每行按 `q.attachment_path` 渲染结果条 |
| 4 | `openPath` 非空错误能显示 | ✅ | `if (reason) setError(reason)`（GeneratedFileResult） |
| 5 | 文件被删除或移动后，定位操作显示明确错误 | ✅ | `showItemInFolder` 失败回 `{ ok:false, reason }` → `setError`；10.4 第 13 条的 `reuseExistingDocPath` 也对「文件已不在」显式不复用 |
| 6 | 长路径正确截断并可查看完整值 | ✅ | SCSS `text-overflow:ellipsis` + `title={artifact.path}` |

**合计**：72 条 → ✅ **64** / 🟡 **3**（10.2-1、10.2-2、10.2-6，均为 §11.2 人工 UI 验收范围） / ⬜ **5**（10.1 第 1 项与 §11.2 的关联项计入前者，其余见下） / ❌ **0**。
—— 折算说明：§10.1 第 1 项「Gate 0 样本采集」是**编码前置条件**而非本刀功能，判 ✅ 但依据在另一任务的工作面文档上；若严格只认本刀可复现证据，应降为 🟡。此项**如实标注，不借文档位次拔高**。

---

## 3. PRD §11 测试计划落实

### 3.1 §11.1 自动测试逐条

| §11.1 要求 | 落实 |
|---|---|
| 新增 `parseBuyerHeader` 纯函数测试 | ✅ `scripts/buyer-header-test.ts`（76/0） |
| 使用 Gate 0 脱敏真实样本 fixture，保留原始格式特征 | ✅ `scripts/fixtures/buyer-header-real.txt`（14 组，覆盖样本分布文档 §3.3 全部异常形态与 §4.3 全部负面标签）。**口径说明**：fixture 是按样本分布文档的**格式特征复刻的合成样本**（数值全为合成值），不是真实样本原文副本——真实样本按 PRD §7.2.6 第 4 条仅本地保存，且其副本所在工作区文件已被另一任务删除 |
| 「名称」命中 /「产品名称」不命中的成对用例 | ✅ 测试 2a/2b + fixture 同名用例 |
| 组合标签检出但不自动拆分 + 仅命中组合标签时 `fields` 全空、`comboHits` 非空、判定失败 | ✅ 测试 5a–5d、6a–6d |
| 客户档案为空直接写入、档案非空才确认、只写入勾选字段 | ✅ 13h（空档案全写）、13i（部分键）、13i2（快照保留未勾选值）、13j（空键不更新）、13f（查询幂等） |
| 标识生命周期：完成/取消后丢弃草稿、残留草稿指向已完成则重新生成、指向未完成则恢复且不重复创建 | ✅ 13k（草稿解析静态断言）+ 页面 [:57-76](src/pages/CrmWorkbenchPage.tsx#L57-L76) 路径复核；**真机刷新场景未实测**（§11.2） |
| 新增 `isPriceOverride` 共享函数边界测试 | ✅ 13a |
| 在 `crm-workbench-test.ts` 增加改价审计断言 | ✅ 13d/13e（含**同源条数断言**） |
| 未改价、显式同价、显式改价和迁移兼容用例 | ✅ 13b（两路不写 `price_overrides`）/ 13c / 13d |
| 前端改价计数与后端审计条数同源断言 | ✅ 13e |
| 数据库幂等查询和跨刷新重试测试 | ✅ 13f/13f2/13g/13g2 |
| 运行现有 docgen 回归 | ✅ **81/0**（本刀新增 3 条复用判定） |
| 运行 TypeScript 检查 | ✅ 根工程 `exit=0`；主进程 156 错误 = 基线 156，**0 新增** |

### 3.2 §11.2 人工 UI 验收（**未实测，如实标注**）

八步全部**未在本机以真实 GUI 点击验证**：仓库无 React 组件测试基础设施（§11.2 首句已声明），本刀只做到「静态接线断言 + 后端单测 + 类型契约」三层。具体未做：切客户清空、粘贴差异化提示的真实观感、行内校验交互、报价失败后从失败点重试、打开/定位/文件不存在三类文件操作。

### 3.3 §11.3 验证命令与真实输出

```
$ npx tsx scripts/buyer-header-test.ts                        → buyer-header: 76 pass / 0 fail
$ npx tsx scripts/crm-workbench-test.ts                       → WORKBENCH RESULT: pass=89 fail=0
$ npm run test:docgen                                         → PASS 81 / 0 FAILED
$ npx tsc --noEmit                                            → exit=0
$ npx tsc -p tsconfig.node.json --noEmit --composite false     → 156 个 error TS（基线 156，0 新增）
```

---

## 4. 真实验证输出（本次全套回归）

| 套件 | 本刀结果 | 前值 |
|---|---|---|
| `crm-workbench-test` | **89 / 0** | 74 / 0 |
| `buyer-header-test` | **76 / 0** | 新增 |
| `crm-docgen-test` | **81 / 0** | 78 / 0 |
| `crm-golden-test` | 47 / 0 | 47 / 0 |
| `crm-claim-test` | 17 / 0 | 17 / 0 |
| `crm-autoconfirm-test` | 58 / 0 | 58 / 0 |
| `crm-lead-test` | 59 / 0 | 59 / 0 |
| `morning-digest-test` | 47 / 0 | 40 / 0 |
| `ai-identify-test` | 48 / 0 | 48 / 0 |
| `insight-noise-test` | 35 / 0 | 35 / 0 |
| `insight-unnamed-session-test` | 9 / 0 | 9 / 0 |
| `settings-nav-test` | 73 / 0 | 58 / 0 |
| `npx vite build` | ✓ 成功 | ✓ |

**已知非回归失败**（与《AI简报与按需识别-实施记录》§3.6/§6.5 记录的失败签名逐字一致，非本刀引入）：`crm-sla-action`（8/3，`1j 完成卡后卡不再是 pending`）、`customer-event-producer`（14/1，`A7 recordQuoteSignal 内部无事件写`）、`customer-event-closed-gate`（15/1，`B11 customer_event 0 行基线`）、`todo-followup` / `crm-logistics`（启动即死于 `completeUnifiedSignal`「请选择具体待办；不能按客户批量完成」）。

---

## 5. 遗留项与未实测

**未实测（不宣称达标）**

1. **真机/人工 UI 验收**——§11.2 八步全部未做（见 §3.2）；10.2 的三条 🟡 同源。
2. **Windows 打包态**——未实测（本机 darwin）。**不以本机启动代替**。
3. **真实刷新恢复链路**——「刷新后依据数据库恢复未完成流程」由静态断言 + 后端单测覆盖，**未在真实浏览器里跑「创建到一半刷新」**。
4. **Gate 0 第 2/5 项**——「六行式/四行式占比统计」按输入口径豁免（样本分布文档 §3.2）；「可提交的脱敏 fixture」的**原始产出**责任在 Gate 0，本刀交付的是其规格落地（合成样本），已如实标注（§3.1）。
5. **合同金额与报价合计的联动**只有静态路径证据（[:344](src/pages/CrmWorkbenchPage.tsx#L344) / [:636](src/pages/CrmWorkbenchPage.tsx#L636)），未做交互实测。

**已知边界（有意为之，非遗留缺陷）**

- 组合标签「不猜测拆分」是**设计决策**（组合标签 1/11，远低于 PRD §7.2.3 的「占比过半」阈值）；联行号不采集（无落点、无消费方），二者均在 PRD/样本分布文档中有据。
- `aiDailyCallLimitEnabled` 开关的启用/停用**不写审计**（AI 简报遗留项口径，见另一份实施记录 §6.7-1），本刀未改此口径。

---

## 6. 契约与文档同步

| 文档 | 变更 |
|---|---|
| `docs/DATA-CONSTITUTION.md` | 新增 2 条登记：① 抬头粘贴解析语义（五键落库语义、值内空白按字段区分、组合标签不写入、失败判定归共享层）；② `ai_daily_limit_change` 审计动作（属 AI 简报遗留项，与合同 PRD 同刀） |
| `docs/API-CONTRACT.md` | §1.6 由 6 通道扩为 **10 通道**：补登 HEAD 已上线但漏登记的 `crm:contract:entryScope` / `:beginEntry` / `:entryQuotation` / `:byCreationRequest`，并补全 `crm:doc:generate` 的 `options.reuseExisting`/`scope`；§1.12 补登 AI 简报与按需识别 6 通道（含 `sales:morningDigest:get` 的 `notReady` 可区分响应），通道数按可核对口径重算为 47（原记 42 与分组相加不符） |
| `docs/HANDOVER.md` | §11 注册本刀新增测试命令与实测计数；主进程类型门禁基线由旧值校正为 **156** |
| 本文件 | 合同 PRD 的实施记录（改动清单 / §10 逐条自查 / §11 落实 / 真实输出 / 遗留项） |

---

## 7. 未提交说明

按任务约束：**未执行任何 `git commit`、未 `git push`**，全部改动留在工作区；**未回退、未提交**工作区中原有的另一任务（隐私清理重构）未提交改动，也未改动其删除/新增的文档路径（含被删除的 PRD v1.4 原文）。
