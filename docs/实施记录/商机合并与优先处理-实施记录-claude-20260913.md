# 商机合并与优先处理（v2.1）实施记录 — claude — 2026-09-13

> 实施契约：`/Users/yang/weflow优化/weflow-商机合并设计稿-v2-20260913.html`（五状态 + 附页「字段来源与交互规则」）
> 任务提示：`/Users/yang/weflow优化/商机合并与优先处理-实施任务提示.md`
> 基线：屏蔽名单改造（`shared/insightBlacklist.ts` 等）与隐私清理/AI 简报/合同等改动均在未提交工作区，本刀**未回退、未提交、未清理**。
>
> **落点说明**：任务提示写的交付路径是 `docs/规划/…-实施记录-claude-20260913.md`。本仓库现行约定把已实施记录统一放 `docs/实施记录/`（`docs/CURRENT.md` §6 明确登记，且两份 2026-09-12 同型记录 `AI简报与按需识别-…`、`合同与报价录入加速-…` 均在彼处），`docs/规划/` 存放的是现行需求/PRD 等开发依据。故本记录按仓库约定落在 `docs/实施记录/`，文件名与两份同型记录保持一致。

---

## 1. 改动清单

### 1.1 新增

| 文件 | 行数 | 职责 |
|------|------|------|
| `shared/opportunitySignals.ts` | 348 | **唯一事实源**：纯函数、零 IO、零 AI。阶段阈值、理由归类、优先层排序、候选资格、以及阶段分析视图的载荷类型 |
| `electron/services/opportunityAnalysisService.ts` | 238 | 只读跨库装配：从 `crmDb`（opportunity / opportunity_event / quote_signal / quotation / crm_risk / intent 记录）+ `salesDb`（follow_up_task / customer_profile）投影出 `OppAssessment[]` 与 `OpportunityAnalysisResult` |
| `src/components/crm/OpportunityStageAnalysis.tsx` | 252 | 阶段分析视图：管道总览 + 漏斗分段 + 最大卡点 + 选中段的优先处理名单（含无会话时禁用「生成跟进建议」的守卫） |
| `scripts/opportunity-analysis-test.ts` | 378 | 新单测 **72 项**（已纳入 HANDOVER §11） |

### 1.2 修改

| 文件 | 改动 |
|------|------|
| `electron/services/crmIpcHandlers.ts` | +4：新增 `crm:opportunity:analysis`（只读，零模型调用、零落库） |
| `electron/services/salesActionEngine.ts` | +93：`getUnifiedSignals()` 中新增「4c. 商机确定性信号汇入」合并遍；新增 `opportunityPriorityScore()` 与 `OPP_REASON_LABEL`；`SignalSource` 增加 `opportunity` 变体 |
| `electron/preload.ts` | 新增 `opportunityAnalysis`；**移除** `funnelStats` |
| `src/types/electron.d.ts` | 新增 `opportunityAnalysis` 与补齐 `actionSuggest` 的类型声明；**移除** `funnelStats` 类型块 |
| `src/pages/OpportunityPage.tsx` | +237：双视图分段控件、`view` 走 URL、两视图各自滚动位置与筛选、优先处理三动作（去跟进 / 建待办·查看待办 / 生成跟进建议）、`TodoForm`、页面级 toast |
| `src/pages/OpportunityPage.scss` | +110：`.opp-viewseg` / `.opp-analysis` / `.opp-pipe` / `.opp-fstage` / `.opp-callout` / `.opp-prow` / `.opp-reason` / `.opp-toast` 等 |
| `src/components/Sidebar.tsx` | CRM 组移除「漏斗」项；lucide 导入移除 `TrendingDown` |
| `src/App.tsx` | 删除 `SalesFunnelPage` 的 lazy 引入；`/sales-funnel` → `RouteStateRedirect to="/opportunities?view=analysis"` |
| `src/pages/TodayActionPage.tsx`、`src/components/sales/AIActionCard.tsx`、`src/stores/todayActionStore.ts` | `opportunity` 来源标签渲染（`source-tag--opportunity`，`商机`） |
| `src/pages/ActionFunnelPage.scss` | 仅注释：移除对已删除 `SalesFunnelPage` 的引用（报表组「行动漏斗」本体未动） |
| `electron/services/salesDbService.ts` | **−83**：删除已失去全部调用方的 `funnelStats()` 及其独占的 `dayKey()` / `orderedDays()`；订正一处口径注释 |

### 1.3 删除（引用已清零）

| 文件 | 行数 | 说明 |
|------|------|------|
| `src/pages/SalesFunnelPage.tsx` | 197 | 能力并入商机「阶段分析」视图 |
| `src/pages/SalesFunnelPage.scss` | 48 | 同上 |
| `src/utils/funnelSummary.ts` | 35 | 仅供 `SalesFunnelPage` 使用 |
| `scripts/funnel-test.ts` | 196 | 被测对象已删 |
| `sales:funnel:stats` IPC（`electron/main.ts`） | — | `funnelStats` 唯一入口，随调用方清零一并删除 |

> 报表组「行动漏斗」(`ActionFunnelPage` / `scripts/action-funnel-test.ts` 25/0) 与 `sales:funnel:stats` 无关，**未动**。
> 全仓检索 `SalesFunnelPage` / `funnelSummary` / `funnel-test` / `funnelStats` / `sales:funnel:stats` 在 `src/`、`electron/`、`shared/`、`scripts/` 下均为 **0 命中**。

---

## 2. 数据语义落点对照表（设计稿附页逐条）

| # | 附页条目 | 附页要求 | 本刀落点 | 结论 |
|---|----------|----------|----------|------|
| 1 | **销售待办到期/逾期** | 已核实存在；`follow_up_task.due_at` = **销售自己做出的**承诺截止，≠ 客户承诺答复时间 | 读 `salesDbService.todoList({status:'pending', session_id})`，按 `due_at` 升序取最近一条 → `task_overdue`（已逾期，附逾期天数）/ `task_due_today`（当日到期）；`due_at` 为空者**不产出**任何到期待办理由，仅计入 `pendingTaskCount`。理由文案固定为「你的待办「<标题>」已逾期 N 天」，来源标注 `待办 #<id>` | ✅ 未把 due_at 说成客户承诺 |
| 2 | **客户承诺答复时间** | 字段不存在，v1 不使用、不新建 | 全仓无该字段、无该理由类型；`OppReasonKind` 不含任何客户承诺语义 | ✅ 未发明 |
| 3 | **报价发出后未获回复** | 唯一合法来源 `quote_signal`（`quoted_at` / `customer_replied_at=0`）；`quotation`/`quote_version` 只证明记录创建，**不得**断言「已发送/未回复」；无法稳定关联时**降级为「报价记录创建 X 天」** | `latestQuoteSignal()` 按 `session_id`→`account_id` 两级关联取最新一条：`customer_replied_at=0` → `quote_unreplied`「报价发出 N 天未获回复」（`hot: true`，来源 `quote_signal（quoted_at MM-DD，customer_replied_at 空）`）；`customer_replied_at>0` → **不出理由**（且不拿 quotation 凑数）；**完全无关联 quote_signal** 时才由 `latestQuotation()` 产出 `quotation_created`「报价记录创建 N 天」（`hot: false`，来源明写「quotation（无 quote_signal 关联，不断言「未回复」）」） | ✅ 断言有据、降级到位 |
| 4 | **竞对 / 风险信号** | `crm_risk` 存量行，零新调用 | `activeRisks()` 读该商机 active 风险行 → `risk` 理由，来源标注风险行 id 与类型 | ✅ 读存量 |
| 5 | **商机口径** | `active`=进行中 / `won`=成交关闭 / `lost`=丢单关闭；**管道与阶段分析仅统计 active**；won/lost 只进独立指标 | `collectOpportunityAssessments()` 的 SQL 硬过滤 `WHERE o.status = 'active'`；`PIPELINE_STAGES = ['了解','比价','决策']`；`wonInWindow(30)` 单独产出 `wonCount30d` / `wonAmount30d`，**不进** `stages` | ✅ active-only |
| 6 | **意向分 / 金额** | `opportunity.intent_score` / `amount_cny` | `intent_score` 直接入 `OppAssessment.intentScore`（排序第四层）；金额口径 `amount_cny > 0 ? amount_cny : amount`（与 `OpportunityPage.tsx` 既有展示口径一致——实测 active 商机 `amount_cny` 恒为 0，仅在成交登记时写入） | ✅ 口径一致 |
| 7 | **阶段滞留阈值** | 了解 7 / 比价 3 / 决策 2 天，写进配置注释可调；成交段不适用 | `shared/opportunitySignals.ts` 顶部 `STAGE_DWELL_DAYS` 常量 + 可调注释；`dwellThresholdDays()` 对成交/流失返回 `null`（不适用滞留） | ✅ |
| 8 | **「去跟进」唤起微信** | deep-link **未核实**，v1 打开应用内会话上下文；如可行列为增强 | 有有效会话（`isSessionIdLike` 三形态校验通过）→ `navigate('/chat?sessionId=…')`；无有效会话 → 打开该商机详情 + toast「未找到与 TA 的聊天会话，已打开商机详情」（**不静默**）。**未做**微信 deep-link，列 §5 遗留项 | ✅ 未做未核实的能力 |
| 9 | **渲染零新 AI 调用** | 名单生成、理由归类全部来自已落库字段 | 阶段分析全链路：纯函数投影 + 只读 SQL，`grep` 确认无 `callChatCompletion` / `simpleCompletion` 调用；唯一 AI 出口是行内「生成跟进建议」按钮的点击事件 | ✅ |
| 10 | **单一聚合链** | 商机确定性信号汇入 `getUnifiedSignals()`；TOP 3 是聚合结果视图截取；不得新增第二套候选/排序/落库管线；合并键先 `opportunity_id` 再 `session_id`/`account_id` | `salesActionEngine.ts` 新增「4c」合并遍：先按 `session_id` 建既有卡索引，商机有同会话卡 → **把全部理由作为 `opportunity` 来源 push 进该卡**（同商机已并入则跳过，幂等），无同会话卡 → 新建 `opp:<id>` 兜底卡。**排序仍只有 `priorityScore` 一条链**，商机分由 `opportunityPriorityScore()` 从 `compareByUrgency()` 的同一优先层映射而来，未引入第二套排序。无任何新表、无新增落库 | ✅ 无旁路 |
| 11 | **理由可追溯** | 每条理由必须能回到具体字段/事件 | `OppReason.source` 强制非空：`待办 #id` / `quote_signal（quoted_at …）` / `quotation（…）` / `阶段记录` / 风险行 id / `意向记录 MM-DD`；渲染层 `.opp-reason__src` 以「来源：…」原样展示 | ✅ |
| 12 | **不展示公式** | 界面只呈现结果与理由 | 优先层排序全部在 `compareByUrgency()` 内完成；`OpportunityStageAnalysis.tsx` 不渲染任何分值、权重或公式；`priorityScore` 仅作卡流排序，未在页面出现 | ✅ |
| 13 | **卡点钟摆** | 滞留数与滞留金额并列展示，不用单一金额防巨单劫持 | `OpportunityStageSegment` 同时输出 `stuckCount` / `stuckAmount`，**不合成综合分**；`isMaxStuck` 仅在**两项同时最大**时为真 | ✅ |
| 14 | **UI 约束** | lucide SVG 图标不用 emoji；按钮 loading/disabled/成功与失败反馈；键盘可操作且有可见焦点；「来源」保证可读对比度；沿用 tokens | 图标全部 lucide（`BarChart3` / `List` / `RefreshCw` / `Sparkles` / `Target` / `ClipboardList` / `AlertTriangle` / `Loader2` 等），全文件 0 emoji；「生成跟进建议」有 `loading` 态与成功/失败两态反馈（失败如实展示错误文案，不静默），「建待办」有 `saving` 态，按钮 `:disabled`；**额外守卫**：商机未关联聊天会话时「生成跟进建议」禁用并附 `title` 说明（该链路会读到空聊天记录，既失真又白花一次模型调用——呼应仓库的 AI 成本治理红线）；分段条用 `aria-pressed`，切换视图的分段控件用 `role="tab"` + `aria-selected`，按钮均为原生 `<button>` 可 Tab 达；`.opp-reason__src` 用 `--color-text-secondary`（非浅灰），无虚线凑数；全部颜色/圆角/阴影取自 `--color-* / --radius-* / --shadow-*` | ✅ |
| 15 | **切换视图保留各自筛选与滚动位置** | 切换 列表 ⇄ 阶段分析 时保留各自筛选与滚动位置 | 视图态走 URL `?view=analysis`（旧链接可分享）；两视图**各有独立 state**（列表：`stageFilter` / `pendingOnly`；分析：`selectedStage`），互不覆盖；滚动位置：`scrollParentOf()` 向上找到 App shell 的 `.content`（`overflow:auto` 的滚动容器不归本页所有），`switchView()` 离开时记录当前视图 `scrollTop`，`useEffect` 在视图变化后（跳过首挂载）回填目标视图的 `scrollTop` | ✅ |

---

## 3. 硬性约束自查

| 约束 | 自查 |
|------|------|
| ⛔ 渲染零新 AI 调用 | 阶段分析视图与 `getUnifiedSignals()` 商机汇入遍均无 AI 调用；`OpportunityStageAnalysis.tsx` 无 `useEffect` 触发 AI；AI 只在「生成跟进建议」`onClick` 内 |
| ⛔ 不建第二套聚合/去重/事实源，不改正式业务字段 | 事实源唯一：`shared/opportunitySignals.ts`（纯函数）；聚合唯一：`getUnifiedSignals()`。本刀**写**操作只有：`salesDbService.todoCreate()`（用户点「建待办」）。`crm/opportunity` 等业务字段**零写入**（见 §4 D 组「聚合只读」断言） |
| ⛔ `enqueueSalesTask` 只加最外层入口；落盘 `atomicWriteFileSync` | 本刀**未新增** `enqueueSalesTask` 调用点，未改动落盘路径 |
| 未新增 AI purpose | 「生成跟进建议」复用既有 `sales.actionSuggest` → `simpleCompletion`，`usageContext.purpose = 'action'`（行动建议生成 · 手动 · salesActionEngine）。**未新增 purpose**，消费清单无需增行（已在 §6 说明） |
| 文案纪律：数字能回到口径 | 管道数/额、滞留数/额一律 active-only；「未回复」必带 `quote_signal` 支撑，无支撑自动降级；未超阈值不标滞留；无阶段事件时措辞降级为「至少 N 天」而非伪精确 |

---

## 4. 验证真实输出（2026-09-13 实测）

### 4.1 类型检查

```
$ npx tsc --noEmit
（无输出）                                              → 0 错误

$ npx tsc -p tsconfig.node.json --noEmit --composite false | grep -c "error TS"
156                                                     → = 基线 156，0 新增

$ npx tsc -p tsconfig.node.json --noEmit --composite false | grep -E "opportunity-analysis-test|opportunitySignals|opportunityAnalysisService"
（无输出）                                              → 本刀新文件 0 错误
```

> ⚠️ 口径提醒（沿用 §2.98）：`shared/**` 同时属于 root 与 node 两个工程，root 工程消费的是 node 工程**已 emit 的 `.d.ts`**。任何 `shared/` 改动后必须跑 `npx tsc -p tsconfig.node.json`（composite 开，隐含 `declaration: true`）重建声明；`--composite false` 只 emit `.js`，会静默留下过期类型。本刀已按此重建（`shared/opportunitySignals.d.ts` 09:53 重新生成）。

### 4.2 构建

```
$ npx tsc -b tsconfig.node.json      → 本刀文件 0 错误
$ npx vite build                     → ✓ built（renderer + dist-electron/preload.js + hermesUtility.js 全部产出）
```

### 4.3 HANDOVER §11 基线（13 套件，逐套件复跑，计数与文档一致）

```
crm-workbench-test        89 / 0        crm-docgen-test            81 / 0
crm-golden-test           47 / 0        morning-digest-test        47 / 0
crm-claim-test            17 / 0        ai-identify-test           48 / 0
crm-autoconfirm-test      58 / 0        insight-noise-test         39 / 0
crm-lead-test             59 / 0        insight-dedup-test         19 / 0
buyer-header-test         76 / 0        insight-unnamed-session     9 / 0
                                        settings-nav-test          73 / 0
```

### 4.4 屏蔽名单等相邻套件（防本刀覆盖上一刀）

```
insight-stage-ban-test      16 / 0      today-action-consumer-test  14 / 0
action-rules-test           36 / 0      action-analysis-judgment    37 / 0
crm-opportunity-test       110 / 0      action-funnel-test          25 / 0
```

### 4.5 本刀新增套件

```
$ npx tsx scripts/opportunity-analysis-test.ts
结果：72 通过 / 0 失败
```

覆盖（对应任务提示 §验证 2 的逐项要求）：

- **A 组 · 事实投影纯函数（40 项）**：`quote_signal` 未回 → 断言「未获回复」+ 标热 + 来源可追溯；已回复 → 不出该理由且**不降级拿 quotation 凑数**；无 `quote_signal` 关联仅有 `quotation` → 降级「报价记录创建 X 天」且**文案不含「未回复」**、`hot:false`、来源明写「无 quote_signal 关联，不断言「未回复」」；两者皆无 → 不编造；待办逾期/今日到期/无期限三分支；阈值常量与成交段 `null`；滞留理由含阈值与天数、**无阶段事件时降级为「至少 N 天」**；候选资格 = 待办到期/逾期 OR 超阈值；优先层排序逐层（逾期时点 → 紧迫信号条数 → 金额 → 意向 → id）含「不受金额劫持」断言；沉默时长取最近真实联系、无记录取 0 不拿创建时刻冒充。
- **B 组 · active-only 口径与报价判定（库装配路径，15 项）**：只装配 5 条 active，won/lost 不在其中；`overview.activeCount/activeAmount` 只含 active；分段不含成交/流失且计数金额与总览配平；won 只进 `wonCount30d/wonAmount30d`（`amount_cny` 口径 152000，不进管道）；报价单经版本链创建（**直写 `quotation` 被宪法 §1.6 守卫拒绝**——测试首版即命中该守卫，已改为 `createQuotation()`）；丙三条理由齐备、阶段滞留不计入「紧迫信号条数」；丁降级文案命中；决策段入段 1 天未超阈值不滞留。
- **C 组 · 最大卡点双指标（7 项）**：了解段滞留 2 条 / 9 万；比价段滞留 2 条 / 20 万；**滞留数并列时由滞留金额决胜 → 仅比价段 `isMaxStuck`**；决策段无滞留故非最大卡点；滞留总数金额配平。
- **D 组 · 汇入单一聚合链与只读（10 项）**：两次调用前后 `opportunity`/`opportunity_event`/`quote_signal`/`crm_risk` 行数与 `follow_up_task` 条数**均不变**（聚合不落库）；二次调用结果稳定（无副作用累积）；同一客户不重复出卡（商机信号并入既有卡且保留该客户原有 `task` 来源）；`sourceRef` 全部非空可追溯；无既有卡的商机新建 `opp:<id>` 卡且来源**全部为 `opportunity`**（不冒充 task/AI）；聚合结果按 `priorityScore` 严格降序（**TOP N 即该序列前缀**）；参与聚合的商机数与阶段分析同源（同一 `collectOpportunityAssessments()`）。

### 4.6 未实测（如实标注）

- 真实 GUI 下的观感、深色主题实际配色、键盘 Tab 顺序与 `:focus-visible` 真机表现——覆盖来自类型检查 + 单测 + 编译期选择器核对，**未在真实运行的应用中人工确认**。
- 双视图滚动位置回填：逻辑依赖向上查找 `.content` 滚动容器，**未在真机长列表上实测**。
- 微信 deep-link 未核实 → 未实现（见 §5）。

---

## 5. 遗留项

| # | 项 | 说明 |
|---|-----|------|
| 1 | 微信 deep-link 唤起 | 设计稿标注「能力待核实」。本刀按 v1 实现为应用内会话上下文 + 降级 toast。**未验证微信是否支持跳到指定会话**，如可行列为增强 |
| 2 | 「查看待办」落点 | 设计稿未指定。本刀落到 `/home`（今日行动 = 待办清单的唯一归属地），**未新增第二套待办视图**。如需精确滚动到该条待办，需 `TodayActionPage` 支持定位参数（本刀未做，避免范围外扩） |
| 3 | 生成的跟进建议无复制按钮 | 行内面板展示了话术与下一步动作，但未提供一键复制（列表页既有复制交互在别的组件里）。未做 |
| 4 | 候选行主标题为 `displayName` | 设计稿示意行展示的是产品/型号行（如「XY-200 改装」）。本刀展示客户名 + 金额 + 意向分，因为 `opportunity.product` 在多条 active 商机上为空，强行展示会出现空标题。如需产品行，先在信号识别侧补齐 `product` 再调整 |
| 5 | `amount_cny` 口径 | 实测 active 商机 `amount_cny` 恒为 0（该字段仅在成交登记时写入），故管道金额走 `amount_cny > 0 ? amount_cny : amount`，与商机列表页既有展示口径一致。**这是既有数据现状，非本刀引入**；如将来 `amount_cny` 在 active 阶段被回填，本口径自动跟随 |
| 6 | 未提交 | 按任务要求，全部改动留工作区，**未 `git commit` / 未 `git push`**。同工作区还并存屏蔽名单、隐私清理、AI 简报、合同等其他任务的未提交改动，一并保持原样 |

---

## 6. AI 调用点变更说明（同步《AI调用入口与消费清单》）

本刀**未新增 AI 调用点、未新增 purpose**。

新增的唯一用户可达 AI 入口是阶段分析名单行内的「生成跟进建议」，其实现为：

```
窗口 actionSuggest  →  sales.actionSuggest
                    →  simpleCompletion({ usageContext: { purpose: 'action' } })
```

`action` 在消费清单中的既有语义即「行动建议生成 · 手动 · salesActionEngine」——已代表「用户手动生成行动/跟进建议」，与本入口的输出与消费场景完全同构。按任务提示 §2 的规则（**若 `action` 已代表该语义，必须复用，不得新增近义 purpose，防止费用统计分裂**），**复用 `action`**。

消费清单的 `action` 行已补注「两处入口（行动卡「生成建议」/ 商机阶段分析「生成跟进建议」），共用同一 IPC 与同一 purpose」，并在表下写明复用理由与无会话禁用守卫，**未新增 purpose 行**。

同时补齐了 `src/types/electron.d.ts` 中一直缺失的 `actionSuggest` 类型声明（此前调用方走 `(window as any)`），使这条既有调用链在类型层面可见——**未改变其运行时行为**。

---

## 7. 文档同步

| 文档 | 同步内容 |
|------|----------|
| `docs/HANDOVER.md` | ① §11 测试命令表新增 `scripts/opportunity-analysis-test.ts`（72/0）与 `crm-opportunity-test`（110/0）、`today-action-consumer`（14/0）分组；② **新增 §2.101** 当前状态小节省（页面合并 / 引用清零 / 事实源 / 口径纪律 / 卡点双指标 / 聚合汇入 / 按需 AI / 去跟进落点 / 测试 / 验证 / 未实测 / 遗留）；③ §3 已交付功能清单：第 28 条「销售漏斗」标注已并入 + 换关键文件，第 35 条去掉「漏斗页下钻」；④ 数据模型 `intent_tag_log` 注更新（表保留、消费方 `funnelStats` 已删）；⑤ 文件地图：`salesDbService.ts` 去 `funnelStats`，`SalesFunnelPage` 条目标注已删除，`funnelPalette` 共用方更新，`OpportunityPage` 补双视图，新增 `OpportunityStageAnalysis.tsx` / `shared/opportunitySignals.ts` / `opportunityAnalysisService.ts` 三条；⑥ 测试索引：`crm-opportunity-test` 计数订正为 110/0，`funnel-test.ts` 标注已删除，新增 `opportunity-analysis-test.ts` 条目；⑦ §12 文档索引新增本记录 |
| `docs/API-CONTRACT.md` | ① 「统计/漏斗（4）」→（3），删除 `sales:funnel:stats` 行并注明退役与替代；② CRM 段新增 `crm:opportunity:analysis` 行（只读、零模型调用、零落库） |
| `docs/规划/AI调用入口与消费清单.md` | `action` 行补注第二入口；表下新增复用说明段（含「未新增 purpose、未新增调用链」与无会话禁用守卫） |
| `docs/实施记录/商机合并与优先处理-实施记录-claude-20260913.md` | 本文件（新建） |

> `docs/CURRENT.md` §6 登记的是「各期盘点 / 收口契约验收 / 已实施计划 / 两份 2026-09-12 实施记录」，本次新增记录与之同类；如需在 CURRENT.md 同步登记一行，属文档导航维护，本刀未改（CURRENT.md 亦仍为未纳版本控制状态，见 §2.100 遗留 ③）。
>
> `docs/归档/**`、`docs/实施记录/**` 的历史小节（§2.14 / §2.15 / §2.24 / §2.78 等）中关于销售漏斗的描述**按原样保留**——它们是历史流水，记录的是当时的真实状态，不在当前阅读路径上；当前状态以 §2.101 为准。
