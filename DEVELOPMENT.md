# AI Agent 软件工程开发规范

本文件是项目级开发规则。Claude Code、Codex 及其他 AI Agent 在参与本项目开发时，必须遵守以下规范。

**文档入口**：查找需求、设计、契约文档先读 `docs/CURRENT.md`（现行权威清单 + 冲突优先级），不要遍历 `docs/`。归档与实施记录默认不读，仅作历史追溯。

⸻

## 一、核心开发原则

### 1. 坚决杜绝闭门造车，避免重复造轮子

在开发新功能、设计新模块或制定较大技术方案时，必须优先考虑：

先搜索 → 再研究 → 再比较 → 再设计 → 最后编码。

禁止在没有进行合理技术调研的情况下，直接从零设计成熟领域已有的解决方案。

⸻

## 二、开源项目调研优先

### 1. 启动较大任务前优先搜索 GitHub

以下情况必须优先进行 GitHub 调研：

- 新项目启动
- 新模块开发
- 核心架构设计
- AI / Agent 功能
- RAG / 知识库
- 搜索系统
- 工作流
- 数据库
- UI 组件体系
- 文件解析
- 消息处理
- MCP / Plugin
- 第三方 SDK
- 已经存在成熟开源实现的功能

优先搜索：

- GitHub
- 官方文档
- 开源项目源码
- 官方 SDK
- 技术社区中的成熟实现

简单 Bug、小范围修改、明确的离线任务可以不强制进行调研。

⸻

### 2. 不要只搜索一个项目

对于重要技术选型，原则上至少寻找 2～5 个候选方案。

重点比较：

- 功能完整度
- 技术栈
- 架构设计
- 数据模型
- 性能
- 稳定性
- 社区活跃度
- 最近更新时间
- Issue / PR 情况
- 安全风险
- License
- 商业使用限制
- 二次开发成本
- 与当前项目的兼容性
- 后续维护成本

不要因为某个项目 GitHub Star 高就直接采用。

⸻

### 3. 优先阅读源码，而不是只看 README

对于具有参考价值的项目，应尽可能分析：

- 项目目录结构
- 核心业务流程
- 数据模型
- API 设计
- 数据库设计
- 状态管理
- AI 调用链
- 插件机制
- 错误处理
- 日志体系
- 测试体系
- 构建方式

需要时直接阅读源码，理解其真正实现，而不是只根据 README 或博客文章判断。

⸻

### 4. 成熟方案优先复用

如果已经存在成熟、稳定、License 合适并且能够满足需求的开源方案：

优先复用，不要重新实现。

可以采用：

- 直接依赖
- 局部集成
- 二次开发
- Fork
- 提取核心实现
- 借鉴架构
- 借鉴数据模型
- 借鉴交互设计

但是必须检查 License 和商业使用限制。

⸻

### 5. 多个开源项目交叉借鉴

不要机械复制单一项目。

应该：

A 项目取架构，B 项目取数据模型，C 项目取交互，D 项目取工程实践。

最终结合当前项目实际需求进行设计。

原则：

取其精华，去其糟粕。

对于存在明显技术债、过度设计、维护停滞或架构缺陷的项目，不得盲目复制。

⸻

### 6. 如果决定重复造轮子，必须有理由

如果 GitHub 已经存在类似实现，但最终仍决定自研，需要明确说明原因，例如：

- License 不适合
- 商业使用受限
- 项目停止维护
- 安全风险
- 技术栈不兼容
- 代码质量较差
- 功能过重
- 二次开发成本过高
- 性能无法满足要求
- 当前业务存在特殊需求
- 自研实现明显更加简单可靠

不能为了“看起来更简单”就默认重新实现成熟功能。

⸻

## 三、设计优先于编码

### 1. 先理解现有项目，再修改

进入已有项目后，不允许直接开始大规模修改。

首先理解：

- 项目目录结构
- 技术栈
- 启动方式
- 构建方式
- 测试方式
- 核心模块
- 数据流
- 依赖关系
- 当前文档
- 当前实现状态

然后再确定修改位置。

⸻

### 2. 优先复用现有架构

新增功能时：

优先寻找现有模块、服务、工具函数、组件、接口和数据结构。

不要因为新需求出现，就随意：

- 创建重复 Service
- 创建重复 Utils
- 创建重复 API
- 创建重复数据库表
- 创建重复组件
- 创建重复状态管理
- 创建第二套相似业务流程

如果已有能力可以扩展，应优先扩展，而不是重新建立一套平行系统。

⸻

## 四、文档同步原则

### 1. 代码、文档、实际状态必须保持一致

以下内容发生变化时，必须主动检查相关文档：

- 功能
- 业务逻辑
- API
- IPC
- 接口协议
- 数据结构
- 数据库 Schema
- 产品交互
- 模块职责
- 项目目录结构
- 配置方式
- 开发方式
- 构建方式
- 测试方式

重点检查：

- README
- AGENTS.md
- CLAUDE.md
- 开发规范
- API 文档
- 数据结构文档
- 架构文档
- 当前状态文档
- 模块 README

⸻

### 2. 文档同步属于开发收尾的一部分

文档同步不能依赖用户提醒。

标准流程：

开发 → 测试 → 文档检查 → 文档更新 → 最终验证 → Commit

在准备告诉用户：

- “完成”
- “测试通过”
- “问题已经解决”
- “可以提交”
- “可以发布”

之前，必须检查文档是否已经过期。

⸻

### 3. 内部实现变化可以不更新文档

如果只是：

- 重构
- 变量重命名
- 内部函数拆分
- 算法优化
- 性能优化
- 代码清理

并且没有影响：

- 使用方式
- API
- 数据
- 交互
- 模块边界
- 开发方式

可以不更新文档。

此时可以明确说明：

无需更新文档，因为本次变更不影响当前使用方式、接口、数据、交互及模块边界。

⸻

### 4. 禁止过期文档继续误导 Agent

如果发现旧文档已经与实际代码不一致，并且可能误导：

- Claude Code
- Codex
- 其他 Agent
- 人工开发者
- 测试人员

必须主动处理。

可以：

- 更新
- 删除
- 归档
- 标记 Deprecated

禁止让过期文档继续处于默认阅读路径。

⸻

### 5. 当前文档描述当前真实状态

当前执行文档应该直接描述：

系统现在是什么。

不要在正文顶部堆积：

- “之前是……”
- “本次改成……”
- “现在调整为……”
- “原方案废弃……”
- “此次修订……”

这种内容属于历史记录，不应该污染当前执行文档。

例如不要写：

原来系统使用 A，经过本次修改后改成 B。

应该直接写：

系统当前使用 B。

⸻

### 6. 历史信息单独保存

只有以下文档适合记录历史变化：

- CHANGELOG
- Migration Notes
- Archive
- 历史设计文档
- 历史技术方案

建议统一放入：

docs/归档/

历史文档不能作为当前开发依据。归档细则（替代文档登记、默认不读规则）见 `docs/归档/README.md`。

⸻

## 五、文档真实性原则

如果出现：

代码 ≠ 文档

必须先判断哪个是真实状态。

情况 A：代码正确

更新文档。

情况 B：文档正确

修正代码。

情况 C：无法判断

停止继续扩展，先确认真实需求。

禁止：

为了让文档看起来正确而修改文档掩盖错误代码。

也禁止：

为了让测试通过而修改文档掩盖错误实现。

⸻

## 六、完成任务前必须进行一致性检查

在宣布任务完成之前，主动检查：

- 功能是否完成
- 测试是否通过
- 是否存在明显回归
- README 是否准确
- 当前架构文档是否准确
- API / IPC 是否准确
- 数据结构是否准确
- 模块职责是否准确
- 开发命令是否准确
- 测试命令是否准确
- 当前状态文档是否准确
- 是否存在过期文档
- 是否存在误导 Agent 的旧说明
- 是否需要归档或删除旧文档

⸻

## 七、开发决策原则

遇到问题时按照以下优先级思考：

第一优先级：复用现有能力

项目里已经存在 → 优先扩展。

第二优先级：复用成熟开源方案

GitHub 已经存在成熟实现 → 优先评估复用。

第三优先级：组合多个成熟方案

单个项目不能满足 → 研究多个项目并组合优势。

第四优先级：自行实现

只有现有项目和开源方案都无法合理满足需求时，才考虑自研。

⸻

## 八、禁止行为

禁止以下行为：

1. 未调研就重新实现成熟功能。
2. 只看一个 GitHub 项目就确定技术方案。
3. 只看 Star 数量判断项目质量。
4. 忽略 License。
5. 忽略安全风险。
6. 忽略项目维护状态。
7. 在已有模块可以扩展时创建重复模块。
8. 为了短期开发速度制造第二套架构。
9. 修改代码后不检查相关文档。
10. 让过期文档继续作为 Agent 默认参考。
11. 用文档修改掩盖代码问题。
12. 用测试通过掩盖业务逻辑错误。
13. 在没有理解现有架构之前进行大规模重构。
14. 为了“看起来更优雅”进行没有实际收益的架构升级。

⸻

## 九、最终开发哲学

整个项目遵循以下原则：

先搜索，再理解；

先复用，再扩展；

先比较，再选型；

先设计，再编码；

代码、测试、文档同步完成；

当前文档只描述当前真实状态；

历史信息单独归档；

能复用的不自研，能扩展的不重建；

用成熟方案降低开发成本，用源码理解保证技术质量。

最终目标不是“写更多代码”，而是：

用最少的重复代码，构建可靠、可维护、可扩展的系统。
